- source_spec: `_bmad-output/implementation-artifacts/1-5-criar-módulos-api-e-mover-as-anotações-de-contrato.md`
  summary: **RESOLVIDO pela Story 1.9.** Mover as anotações de validação (`@CPF`, `@CNPJ`, `@TaxIdentifier`, `@ZipCode`) para o módulo `validation-api` ficava bloqueado até a Story 1.9 extrair os `ConstraintValidator` (`CnpjValidator`, `CpfValidator`, `TaxIdentifierValidator`, `ZipCodeValidator`) de `utils` para o módulo `validation`.
  evidence: As anotações usam `@Constraint(validatedBy = XValidator.class)`, uma referência de compilação ao validador concreto. Movê-las sozinhas para `validation-api` (dependência única permitida `jakarta.validation-api`) cria um ciclo `utils → validation-api → utils` e quebra a AC #1 da Story 1.5. Esvaziar `validatedBy` exige mapeamento manual via `META-INF/validation.xml`, o que é lógica nova fora do escopo de "mover código" e quebraria `ZipCodeValidatorTest`. Decisão do usuário (2026-08-22): adiar a Task 3 até a Story 1.9 destravar o caminho natural. `validation-api/pom.xml` foi criado como placeholder (só dependência `jakarta.validation-api`, registrado em `<modules>` do pom raiz), sem anotações, sem README, sem regra ArchUnit — completar isso é o trabalho pendente aqui. **Fechado pela Story 1.9**: as 4 anotações migraram para `validation-api` com `validatedBy = {}` + `META-INF/validation.xml`/`validation-constraint-mappings.xml` em `validation` (ver entrada abaixo sobre o risco desse mapeamento XML), `ArchitectureTest` local e `README.md` adicionados.

- source_spec: `_bmad-output/implementation-artifacts/1-5-criar-módulos-api-e-mover-as-anotações-de-contrato.md`
  summary: O `jacoco-check` do perfil `analyze` falha com 0% de cobertura em `audit-api` durante um rebuild `-am` completo, mesmo com os 2 testes ArchUnit do módulo passando — provavelmente afeta `jdempotent-api` (mesmo padrão de módulo só-anotação) e vai afetar `validation-api` quando a Story 1.9 o preencher.
  evidence: Surgiu incidentalmente durante a revisão da Story 1.6 (`archtest`), que roda `-am` e arrasta `audit-api` no rebuild. Reproduzido de forma independente: `mvn -o -pl audit-api -am -Panalyze -Ddependency-check.skip=true verify` → a execução `check` do jacoco reporta "All coverage checks have been met", mas a execução `jacoco-check` no mesmo módulo reporta "Rule violated for bundle scos-foundation-audit-api: instructions covered ratio is 0.00, but expected minimum is 0.80" e falha o build. Os testes ArchUnit (`ArchitectureTest`) rodam e passam — o problema é que reflexão/import de classes via ArchUnit não gera instructions cobertas pelo agent do JaCoCo, então um módulo 100% anotação/enum nunca bate o mínimo de 80%, mesmo com teste real cobrindo a única regra de negócio do módulo (a regra ArchUnit em si). Não é causado pela Story 1.5 nem pela 1.6 — é uma lacuna na configuração do perfil `analyze` (Story 1.4) para módulos sem lógica executável. Vale uma exclusão/threshold dedicado para módulos `*-api`/contrato no `jacoco-maven-plugin` do pom raiz. Consequência prática: bloqueia `mvn -B -Panalyze test` (o comando exato do job `security-check` do CI, ver `.github/workflows/build.yml`) antes de alcançar qualquer módulo declarado depois de `audit-api` no reactor, incluindo `archtest` (Story 1.6) — confirmado rodando esse comando literal, que falha em `audit-api`. Separadamente, `mvn -B -Panalyze test` também falha em `jdempotent` por erro de Testcontainers/Docker Compose neste sandbox de desenvolvimento (sem Docker disponível) — não confirmado se é limitação só deste ambiente ou também afeta o runner do CI real; não registrado como item de trabalho porque não há evidência de ser um bug do repositório. **Ver também a entrada da Story 1.10 abaixo**: em um ambiente com Docker disponível, o mesmo teste falha por um motivo diferente (fragilidade de porta fixa), não por ausência de Docker.

- source_spec: `_bmad-output/implementation-artifacts/1-7-extrair-o-módulo-core.md`
  summary: Duas implementações paralelas de "capitalize"/"camelCase" agora existem sem fonte única de verdade — `StringTransformRule` (movida de `utils` para `spring` na Story 1.8, ainda usa `org.springframework.util.StringUtils`) e `core.StringFieldUtils` (usa `org.apache.commons.lang3.StringUtils`) — podem divergir silenciosamente em casos de borda (locale, espaços múltiplos).
  evidence: `StringFieldUtils.applyTransformation` movida para `core` (Story 1.7) teve que trocar `StringTransformRule` por `UnaryOperator<String>`, reimplementando as 4 transformações com `commons-lang3` para não recriar a dependência de Spring em `core`. `StringTransformRule` continua existindo (agora em `spring`, para onde migrou junto de `NormalizeStrings` na Story 1.8 — ver Debug Log dessa story) com sua própria implementação (ainda redundante) das mesmas 4 transformações. Não corrigível dentro do escopo de nenhuma das duas stories de extração (moveria lógica de negócio além de "mover"); resolve-se quando/se `StringTransformRule` for descontinuada em favor de `core.StringFieldUtils` diretamente, ou quando algum épico futuro tratar a consolidação.

- source_spec: `_bmad-output/implementation-artifacts/1-8-extrair-o-módulo-spring.md`
  summary: O inventário congelado da Story 1.1 (`inventario-classe-modulo.md`) está desatualizado em 2 entradas — `specification.ScosStartupListener` (linha 5, nota explícita na linha 104) e `enums.StringTransformRule` (linha 35) — ambas listadas com destino `core`, mas ambas terminaram em `spring` (a primeira já na Story 1.7/1.8 por AC explícito, a segunda por necessidade técnica descoberta na própria Story 1.8). Nenhuma das duas stories atualizou o arquivo congelado.
  evidence: O preâmbulo do próprio arquivo diz que Stories 1.5–1.13 devem apontar para ele "em vez de recitar a tabela" — uma entrada desatualizada é um risco real de uma story futura (1.9–1.15) confiar cegamente no destino errado. Não é causado exclusivamente pela Story 1.8 (a divergência do `ScosStartupListener` já vem da 1.7); resolve-se atualizando as 2 linhas + a nota da seção "Pontos de lacuna resolvidos" para refletir o destino real, ou anotando-as como supersedidas.

- source_spec: `_bmad-output/implementation-artifacts/1-8-extrair-o-módulo-spring.md`
  summary: `ScosOnStartupListener.onApplicationEvent` itera `scosStartupListener.forEach(...)` sem try/catch por listener — se uma implementação de `ScosStartupListener` lançar exceção, os listeners restantes são pulados e o log final ("- Final") nunca é emitido.
  evidence: Comportamento pré-existente, migrado verbatim de `utils` para `spring` nesta story (zero mudança de lógica, conforme AC). Encontrado pelo Edge Case Hunter durante a revisão desta story; não é regressão introduzida agora, mas nunca foi corrigido porque nunca houve teste cobrindo múltiplos listeners com falha parcial.

- source_spec: `_bmad-output/implementation-artifacts/1-8-extrair-o-módulo-spring.md`
  summary: `ScosOnStartupListener` loga `"InsideSoftwaresOnStartupListener#onApplicationEvent() - Init/Final"` — um nome de classe antigo/incorreto (não é `ScosOnStartupListener`), preservado verbatim na migração.
  evidence: Comportamento pré-existente em `utils` antes da Story 1.8, só re-empacotado (zero mudança de lógica, conforme AC). Encontrado pelo Blind Hunter durante a revisão desta story; corrigir a string de log é trivial mas está fora do escopo de "mover" desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-8-extrair-o-módulo-spring.md`
  summary: `StringTransformRule.CAPITALIZE.apply(value)` produz um espaço inicial indesejado quando `value` tem espaço em branco no início, porque `value.split("\\s+")` gera um primeiro token vazio.
  evidence: Comportamento pré-existente, migrado verbatim de `utils` para `spring` nesta story (zero mudança de lógica, conforme AC). Encontrado pelo Edge Case Hunter durante a revisão desta story; nenhum teste em `StringTransformRuleTest` cobre entrada com espaço em branco líder.

- source_spec: `_bmad-output/implementation-artifacts/1-9-extrair-o-módulo-validation.md`
  summary: `ScosExceptionCode` nunca sobrescreve `ExceptionCode.getHttpCode()` (default `400`), contradizendo o próprio Javadoc do enum, que documenta `ACCESS_DENIED` como HTTP 403, `TOKEN_NOT_PROVIDED` como 401 e `GENERIC` como 500 — qualquer `new ScosException(ExceptionCode)` construído diretamente com esses códigos reporta 400 em vez do status documentado.
  evidence: Comportamento pré-existente, confirmado por diff byte-a-byte contra a versão anterior em `utils.enums.ScosExceptionCode` (movida para `core` nesta story sem nenhuma mudança de lógica). `ACCESS_DENIED`/`GENERIC` não sofrem na prática hoje porque `ExceptionsHandler` monta a resposta HTTP desses 2 casos com `HttpStatus` fixo no código (não via `getHttpCode()`); `TOKEN_NOT_PROVIDED` não é lançado como `ScosException` em nenhum lugar do repo atualmente. `ScosExceptionCodeTest` (também movido) só testa 2 dos 9 valores do enum, o que deixou o bug passar despercebido. Risco real só se materializa se algum código futuro (neste repo ou em consumidor externo) fizer `new ScosException(ScosExceptionCode.ACCESS_DENIED)`/`TOKEN_NOT_PROVIDED`/`GENERIC` diretamente. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-9-extrair-o-módulo-validation.md`
  summary: `validation` introduz o primeiro `META-INF/validation.xml` do repositório (para religar `validatedBy = {}` das anotações de `validation-api` aos validadores concretos, evitando o ciclo de módulo). Jakarta Bean Validation normalmente só honra um `validation.xml` por bootstrap — uma aplicação consumidora que já tenha o seu próprio arquivo pode colidir silenciosamente, desativando `@CPF`/`@CNPJ`/`@TaxIdentifier`/`@ZipCode` sem erro nem aviso. Depender só de `validation-api` (sem `validation`) também compila e "valida" sem nenhuma violação, silenciosamente — risco hoje documentado apenas em prosa no `README.md`, sem nenhuma salvaguarda automática (ArchUnit/teste/build).
  evidence: Confirmado que nenhum outro módulo do repo usa esse padrão (`find . -name validation.xml` só retorna o novo arquivo). Testes atuais (`TaxIdentifierAnnotationsValidatorTest`, etc.) só inicializam a Bean Validation diretamente (`Validation.byDefaultProvider()`), não via `spring-boot-starter-validation`/`LocalValidatorFactoryBean`, que é o caminho real de qualquer app consumidora — o cenário de colisão nunca é exercitado. Encontrado pelo Blind Hunter e pelo Edge Case Hunter, independentemente, durante a revisão desta story. Mitigação futura possível: registrar o `ConstraintMapping` programaticamente (`Configuration.addMapping(...)`) em vez de depender do único `META-INF/validation.xml` descoberto pelo classloader.

- source_spec: `_bmad-output/implementation-artifacts/1-10-extrair-o-módulo-cache.md`
  summary: Aplicações consumidoras de `jdempotent` que dependiam apenas dele deixam de receber um provedor Jakarta Bean Validation (`hibernate-validator` ou equivalente) transitivamente — antes vinha de graça via `jdempotent → scos-foundation-utils → spring-boot-starter-validation`; agora `jdempotent → scos-foundation-cache`, que deliberadamente só traz `jakarta.validation-api` (contrato, sem implementação, conforme AC #1 desta story). Como `ScosCacheConfiguration`/`ScosCacheProperties` (`@Validated`, `@ConfigurationProperties`) tem `@ConditionalOnProperty(matchIfMissing = true)` — ativo por padrão — uma app consumidora real que não traga seu próprio provedor JSR-380 falha na subida com `NoProviderFoundException`, não silenciosamente.
  evidence: Mesma categoria de risco já registrada acima para `validation-api`/`validation` (Story 1.9) — perda de uma dependência transitiva de validação ao trocar `utils` por um módulo-folha mais estreito. Diferença aqui: a falha é barulhenta (app não sobe) em vez de silenciosa, o que reduz mas não elimina o risco (só descoberto em produção/deploy, não em tempo de compilação). Confirmado via `mvn -pl jdempotent -am test -Dtest=PrimeNumbersJdempotentEnableTest,PrimeNumbersJdempotentDisableTest`: o contexto Spring sobe corretamente no próprio `jdempotent` porque seu `pom.xml` de teste ganhou `hibernate-validator` como dependência de **teste** (ver Debug Log da Story 1.10) — mas esse fix é local ao harness de teste, não resolve o caso de uma aplicação real que só declare `scos-foundation-jdempotent` em produção. Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-9-extrair-o-módulo-validation.md`
  summary: `@QueryEmbeddable` foi removido de `Cnpj` durante a migração para `validation` (a lista de dependências da AC não inclui QueryDSL) — nenhum consumidor interno ao repo foi encontrado, mas é uma classe de uma lib "Foundation" com consumidores externos conhecidos (`README.md` cita SCOS-Flow); se algum consumidor externo embute `Cnpj` numa entidade JPA própria e usa QueryDSL para navegação tipada, essa é uma mudança quebradora não comunicada fora deste repositório.
  evidence: `grep` no repo inteiro confirmou zero consumidores de `QCnpj` (a Q-class gerada pelo APT do QueryDSL). Manter a anotação exigiria adicionar `querydsl-core`/`querydsl-apt` a `validation/pom.xml`, fora da lista de dependências que a própria AC #1 desta story autoriza (`core`, `validation-api`, `caelum-stella-core`, `jakarta.persistence-api` provided). Encontrado pelo Blind Hunter durante a revisão desta story; não verificável sem acesso ao código de consumidores externos como SCOS-Flow.

- source_spec: `_bmad-output/implementation-artifacts/1-10-extrair-o-módulo-cache.md`
  summary: **Atualização da entrada da Story 1.5 acima sobre Testcontainers/Docker Compose em `jdempotent`**: neste ambiente (sessão da Story 1.10) o Docker está disponível e um `docker compose up -d` manual do mesmo `jdempotent/src/test/resources/docker-compose.yml` funciona isoladamente — mas os 2 testes que dependem dele (`PrimeNumbersJdempotentDisableTest`, `PrimeNumbersJdempotentEnableTest`) continuam falhando via Testcontainers (`ContainerLaunchException`/"Docker Compose exited abnormally"/timeout de porta), reproduzido de forma consistente em 3 execuções separadas. Não é falta de Docker (como a entrada original registrou para aquele sandbox) — é fragilidade de portas fixas (`6379`, `26379`) publicadas no compose entre execuções consecutivas de teste dentro do mesmo `mvn test`.
  evidence: Confirmado que a causa não é relacionada ao código movido nesta story: (1) o `docker-compose.yml` não referencia `PolymorphicRedisSerializer`/`ScosCacheConfiguration` nem qualquer classe movida; (2) `docker compose up -d`/`down -v` manual do arquivo funciona sem erro; (3) a falha se repete de forma idêntica mesmo após remover manualmente containers `redis`/`redis-sentinel` órfãos entre tentativas. Ambas as classes de teste usam o mesmo par de portas fixas do compose (em vez de portas dinâmicas do Testcontainers), o que é uma fragilidade de design do teste — não registrado como bug a corrigir agora (fora do escopo desta story de extração de módulo), mas caso uma story futura mexa em `jdempotent`/Testcontainers, vale considerar portas dinâmicas ou `Testcontainers.exposeHostPorts`/`ComposeContainer.withExposedService` em vez de publicar porta fixa.

- source_spec: `_bmad-output/implementation-artifacts/1-11-extrair-o-módulo-jpa.md`
  summary: Nenhuma salvaguarda automática (teste/build) impede que o FQN de uma classe movida, referenciado como **string literal** num arquivo de configuração (YAML/properties) em vez de import Java, quebre silenciosamente na próxima extração de módulo — o padrão de erro já se materializou uma vez nesta story (`hibernate.type.json_format_mapper` apontando para o pacote antigo de `JacksonCustomJsonFormatMapper`, causando falha em runtime só detectada pelos testes de `audit`, não pela compilação).
  evidence: A regressão desta story foi pega porque os testes de `audit` sobem um `EntityManagerFactory` real (`@SpringBootTest` com perfil `postgres`) — se um módulo futuro mover uma classe referenciada por FQN-string numa config sem esse tipo de teste de bootstrap, a quebra passaria despercebida até produção. `grep` confirma que não há mais nenhuma ocorrência desse padrão no repo hoje (Verification Gap Reviewer desta story confirmou), mas nada impede que reapareça nas extrações restantes (`web`, `feign`, Épico 1.14). Mitigação futura possível: um teste/step de CI que faça `grep` por FQNs de classes movidas em `**/*.yml`/`**/*.yaml`/`**/*.properties` sempre que uma story de extração mover uma classe, ou just documentar a checagem como parte do checklist de cada story de extração restante.

- source_spec: `_bmad-output/implementation-artifacts/1-12-extrair-o-módulo-web.md`
  summary: Nenhum teste do reactor confirma que aplicações consumidoras reais (com `@ComponentScan`/`@SpringBootApplication` de `basePackages` potencialmente restrito) continuam descobrindo os beans movidos (`ScosController`, `ScosJacksonConfig`, `LoggingInitialFilter`/`LoggingFinalFilter`) agora que o pacote raiz mudou de `br.com.sawcunhaos.foundation.utils.*` para `br.com.sawcunhaos.foundation.web.*`.
  evidence: Mesma categoria de risco já registrada para `validation-api`/`cache` (Stories 1.9/1.10) — mudança de pacote em módulo-folha pode quebrar silenciosamente um consumidor externo cujo `@ComponentScan` não escaneia `br.com.sawcunhaos.foundation` de forma ampla (ex.: escaneia só um sub-pacote específico esperando `utils`). Encontrado pelo Blind Hunter durante a revisão desta story; não verificável dentro deste repo (nenhuma aplicação consumidora real está presente para testar).

- source_spec: `_bmad-output/implementation-artifacts/1-12-extrair-o-módulo-web.md`
  summary: `ScosRequestAnnotationCachingTest` (novo) verifica que `@Cacheable`/`@CacheEvict` são resolvidos corretamente via `AnnotatedElementUtils.findMergedAnnotation` (metadados de anotação), mas nenhum teste do módulo `web` sobe um `ApplicationContext`/`CacheManager` real para confirmar que o cache de fato funciona em runtime (hit/miss, eviction) depois da migração de pacote.
  evidence: Escolha consciente de escopo — a AC #1 pede para confirmar que `nameCache()` continua *resolvendo* `@Cacheable` corretamente após a migração, e é exatamente isso que a migração de pacote poderia quebrar (não o comportamento de cache em si, que não muda com a localização do arquivo). Um teste de integração completo (`@SpringBootTest` com Redis real ou embutido) cobriria mais, mas está fora do escopo desta story de extração. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-14-impor-regras-archunit-cross-módulo-e-remover-utils-do-reacto.md`
  summary: **Reconfirmação da entrada da Story 1.10 acima** — `PrimeNumbersJdempotentDisableTest`/`PrimeNumbersJdempotentEnableTest` (portas fixas `6379`/`26379` no `docker-compose.yml` de teste) continuam falhando de forma flaky (`ContainerLaunchException: Bind for 0.0.0.0:6379 failed: port is already allocated`) mesmo agora, com `utils` totalmente removido do reactor.
  evidence: `git diff --stat` entre o `baseline_commit` desta story e o `HEAD` confirma zero mudança em `jdempotent/` — o módulo é byte-a-byte idêntico ao ponto anterior à remoção de `utils`, então a falha reproduziria de forma idêntica no baseline. Reproduzido tanto no `mvn -o test` do reactor completo quanto isolando `mvn -o -pl jdempotent test`, com Docker disponível e nenhum container/processo prévio ocupando as portas 6379/26379 no host (confirmado via `docker ps -a` e checagem de sockets). Confirma definitivamente que não é causado por nenhuma story de extração — é fragilidade estrutural do fixture de teste (portas fixas em vez de dinâmicas), como já apontado na Story 1.10. Como esta é a última story do bloco de extração modular do Épico 1, vale abrir um item de trabalho dedicado (fora do Épico 1) para trocar as portas fixas do `docker-compose.yml` por portas dinâmicas/`Testcontainers.exposeHostPorts`, em vez de deixar essa flakiness se acumular como "conhecida" story após story.

- source_spec: `_bmad-output/implementation-artifacts/1-14-impor-regras-archunit-cross-módulo-e-remover-utils-do-reacto.md`
  summary: A documentação de configuração para consumidores (`README.md` raiz, `audit/README.md`, `etc/doc/skills/README.md`, `etc/doc/skills/scos-utils-config/SKILL.md`, `etc/doc/commit-convention.md`) continua descrevendo `scos-foundation-utils` como módulo existente/base obrigatório — inclusive com snippets `<dependency><artifactId>scos-foundation-utils</artifactId></dependency>` que agora falham a resolver, e um fluxo de setup (`etc/doc/skills/README.md`, seção "Ordem de setup") que instrui `utils` como primeiro passo obrigatório para qualquer sistema novo. Isso não é mais "desatualizado", é **ativamente quebrado**: qualquer app nova ou skill de configuração seguida hoje falha na resolução de dependência Maven.
  evidence: Confirmado lendo os 5 arquivos diretamente (`README.md:15,143-200,661,721`; `audit/README.md:92,111-112` com imports `utils.annotation.audit.{Auditable,AuditAction}` que na verdade vivem em `audit-api` desde a Story 1.5; `etc/doc/skills/README.md` linhas 9 e 17-18; `etc/doc/skills/scos-utils-config/SKILL.md` inteiro, ~documento de skill completo dedicado a um módulo que não existe mais; `etc/doc/commit-convention.md:30`). Encontrado pelo Blind Hunter durante a revisão desta story. **Decisão consciente de não corrigir agora**: a Dev Note desta própria story (linha 40) descreve a Fase 5 do plano de origem como "última fase antes do piso de documentação do Épico 4" — reescrever corretamente o `README.md` (redistribuindo a lista de features do antigo `utils` entre `spring`/`validation`/`cache`/`jpa`/`web`/`feign`/`core`) e o `scos-utils-config/SKILL.md` (provavelmente virando 6-7 skills novas, uma por módulo-folha) é trabalho de documentação substancial, não um patch pontual — e nem a AC nem os Tasks desta story pedem isso. Sinalizado aqui como **bloqueante para o piso de documentação do Épico 4** (não cosmético), para não ser perdido de vista.

- source_spec: `_bmad-output/implementation-artifacts/1-14-impor-regras-archunit-cross-módulo-e-remover-utils-do-reacto.md`
  summary: `.github/workflows/build.yml` nunca ganhou steps de "Archive/Publish module artifacts" para nenhum dos 8 módulos criados ao longo do Épico 1 (`core`, `spring`, `validation`, `cache`, `jpa`, `web`, `feign`, `archtest`) — só existem steps dedicados para `privacy`/`exception`/`audit`/`jdempotent` (pré-existentes) e, até esta story, `utils`. As duas steps de `utils` (`Archive utils module artifacts` no job `build`, `Publish utils module security report` no job `security-check`) foram removidas nesta story por apontarem para um diretório que literalmente não existe mais (`utils/target/`) — isso sim é consequência direta da remoção desta story, corrigido.
  evidence: `grep -n "utils" .github/workflows/build.yml` confirmava as duas steps antes da correção; `grep` pelos nomes dos 8 módulos novos não retorna nenhuma step "Archive"/"Publish" correspondente — gap acumulado desde a Story 1.6 (`archtest`), nunca fechado por nenhuma story de extração seguinte. Não é regressão desta story (o gap já existia para `core`..`feign` antes de 1.14 tocar o arquivo), mas como as steps de `build`/`security-check` já rodam `mvn -B test`/`mvn -B -Panalyze test` na raiz (cobrindo o reactor inteiro funcionalmente), o único efeito prático da lacuna é a ausência de artifact upload individual por módulo para debug/download no CI — não perda de cobertura de teste/análise. Vale abrir item de trabalho dedicado para adicionar as 8 steps faltantes (ou refatorar para uma matrix/loop em vez de um step hardcoded por módulo, o que evitaria esse gap se repetir a cada módulo novo). Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-14-impor-regras-archunit-cross-módulo-e-remover-utils-do-reacto.md`
  summary: `archtest`'s duas regras cross-módulo (`nothingDependsOnWeb`, `noCyclesBetweenModules`) só enxergam classes de `src/main/java` de cada módulo dependência (`ImportOption.DoNotIncludeTests` + dependências `<scope>test</scope>` sem classifier `test-jar`) — nenhuma classe de `src/test/java` de nenhum módulo entra na análise. Uma violação (dependência indevida de `web`, ou um ciclo) que só existe em código de teste passa pelas duas regras sem ser detectada.
  evidence: Confirmado lendo `archtest/pom.xml` (dependências normais, sem `<classifier>test-jar</classifier>` nem `<type>test-jar</type>`) e `ArchitectureTest.java` (`ImportOption.DoNotIncludeTests.class`). Realístico, não hipotético: o próprio fix desta story em `LocaleUtilsBean.java` (`audit/src/test/java`) era exatamente uma classe de teste dependendo silenciosamente da coisa errada, e teria escapado das duas regras cross-módulo mesmo depois de existirem. Corrigir exigiria publicar test-jars de cada módulo (mudança de packaging não-trivial, afeta todo o reactor) ou aceitar a lacuna como escopo consciente (regras cross-módulo cobrem só código de produção, coerente com o objetivo de proteger a API pública do módulo). Encontrado pelo Edge Case Hunter durante a revisão desta story; nem a AC #1 nem a #2 desta story mencionam escopo de teste explicitamente, então não é um gap em relação ao que foi pedido — é uma lacuna estrutural do próprio ArchUnit test-jar publishing, fora do escopo de decidir/corrigir nesta story.

- source_spec: `_bmad-output/implementation-artifacts/1-14-impor-regras-archunit-cross-módulo-e-remover-utils-do-reacto.md`
  summary: A regra `noCyclesBetweenModules` usa `slices().matching("br.com.sawcunhaos.foundation.(*)..")`, que colapsa cada módulo `-api` (`audit-api`, `jdempotent-api`, `validation-api`) na mesma "slice" do seu módulo-pai (`audit`, `jdempotent`, `validation` respectivamente, já que compartilham o primeiro segmento de pacote). Um ciclo real entre um módulo `-api` e seu próprio pai nunca seria detectado por esta regra, porque ambos são contados como a mesma slice.
  evidence: Confirmado lendo os pacotes reais (`audit-api` usa `br.com.sawcunhaos.foundation.audit.api`, mesmo primeiro segmento `audit` que o módulo `audit`). **Mitigação já existente, verificada**: `audit-api`/`jdempotent-api`/`validation-api` já têm, desde a Story 1.5, uma regra ArchUnit local `noRuntimeDependencyBeyondJdk(AndJakartaValidation)` (`classes().should().onlyDependOnClassesThat().resideInAnyPackage("java..", ..., "br.com.sawcunhaos.foundation.<x>.api..")`) que proíbe o próprio código do `-api` de importar qualquer coisa do módulo-pai — isso já fecha o caminho de código que um ciclo real exigiria, mesmo com a slice colapsada. O ponto cego residual é só o `pom.xml` do `-api` declarar uma dependência Maven não utilizada por nenhuma classe (peso morto, não ciclo de fato) — risco bem menor do que o inicialmente levantado. Não vale regra adicional agora; registrado só para não perder o raciocínio. Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-15-emitir-log-de-inicialização-por-módulo-de-implementação.md`
  summary: As 3 regras de contagem (`JdempotentAnnotationCountListener`/`AuditableAnnotationCountListener`/`ValidationAnnotationCountListener`) usam `ClassPathScanningCandidateComponentProvider`, que só retorna classes concretas — uma classe abstrata ou interface anotada (ex.: `@Auditable` numa entidade base abstrata de uma hierarquia JPA, um padrão comum) nunca vira candidata de scan, então nunca é contada. Sub-contagem silenciosa, sem log nem aviso.
  evidence: Confirmado lendo o comportamento de `isCandidateComponent` de `ClassPathScanningCandidateComponentProvider` (Spring Framework): exige `metadata.isIndependent() && (metadata.isConcrete() || ...)`, que exclui interfaces e classes abstratas por definição. Corrigir exigiria não usar o scanner padrão do Spring (que aplica esse filtro) e ler os metadados via ASM diretamente sem esse filtro — mudança maior, fora de proporção para a última story do épico. Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-15-emitir-log-de-inicialização-por-módulo-de-implementação.md`
  summary: As 3 mesmas regras fazem `Class.forName(name, false, classLoader)` + reflexão em **toda classe concreta** do(s) pacote(s) base da aplicação consumidora, em **toda subida** da aplicação — sem cache entre execuções, sem opt-out (`@ConditionalOnProperty` só liga/desliga o bean inteiro, não reduz o escopo do scan), sem amostragem. Se uma app consumidora tiver `audit` + `jdempotent` + `exception` simultaneamente (combinação plausível), o mesmo pacote base é escaneado 3 vezes de forma independente.
  evidence: Confirmado lendo o código dos 3 listeners — o loop `for (String className : scannedClassNames) { Class.forName(...); ... }` não tem limite de tamanho nem cache. `Class.forName` com `initialize=false` ainda faz load+link completo (inclui verificação de bytecode), custo real por classe. Mitigação futura mais robusta seria reescrever para ler metadados via ASM cru (bytecode) em vez de `Class.forName`, evitando load de classe por completo — mas a alternativa mais óbvia e já disponível no Spring (`AnnotationMetadata` baseado em ASM, usado internamente pelo próprio `ClassPathScanningCandidateComponentProvider`) não expõe anotação em nível de campo, então não resolve `@JdempotentId`/`@CPF`/`@CNPJ`/`@TaxIdentifier`/`@ZipCode` sem um `ClassVisitor` customizado (`visitField`) escrito à mão — engenharia não-trivial, fora de escopo desta story. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-15-emitir-log-de-inicialização-por-módulo-de-implementação.md`
  summary: `countAnnotatedElements` (scanner + `Class.forName` + reflexão + tratamento de erro) está duplicado quase verbatim nos 3 listeners (`jdempotent`, `audit`, `exception`), diferindo só em qual(is) anotação(ões) procurar. Escolha deliberada desta story (documentada no Debug Log): não criar um módulo novo compartilhado só para isto, já que os 3 módulos-alvo não têm relação de dependência entre si hoje.
  evidence: Comparação direta dos 3 arquivos `*AnnotationCountListener.java` confirma a duplicação. Se um bug for encontrado num dos 3 no futuro (como os 4 já corrigidos nesta própria story: falta de `try/catch` em `AutoConfigurationPackages.get()`, ausência de dedup, log level do catch, falta de `AutoConfiguration.imports`), precisa ser corrigido nos 3 lugares independentemente — já aconteceu dentro desta mesma story. Consolidar exigiria um módulo novo de utilidade compartilhada (ou mover a lógica para `core`), decisão de arquitetura fora do escopo de decidir ad-hoc nesta story. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-1-congelar-o-inventário-classe-módulo.md`
  summary: **Revisão retroativa (Épico 1 fechando)**: `inventario-classe-modulo.md` está desatualizado em pelo menos 5 linhas, não só as 2 já registradas na entrada da Story 1.8 (`ScosStartupListener`/`StringTransformRule`). Confirmado ao vivo: `DateUtils`/`HashUtils`/`StringFieldUtils` estão hoje em `br.com.sawcunhaos.foundation.core.utils`, não em `br.com.sawcunhaos.foundation.core` (raiz) como a tabela promete e como a própria narrativa da Completion Notes afirma explicitamente ter evitado ("realocadas para a raiz do pacote... em vez de recriar um pacote-saco").
  evidence: Confirmado lendo os pacotes reais das 3 classes no reactor atual. Nenhuma story posterior corrigiu a tabela nem existe mecanismo de imposição (ex.: uma regra ArchUnit que compare o layout real contra a tabela) — o documento "congelado" já diverge da realidade em ~7% das suas 72 linhas e continua rotting silenciosamente. Encontrado pelo Edge Case Hunter durante a revisão retroativa desta story (nunca tinha passado pela etapa de revisão do workflow antes do fechamento do Épico 1). Não corrigido nesta revisão — corrigir a tabela em si é trabalho de documentação, não um bug de código; registrado para uma story futura de manutenção do inventário (ou para ser levado em conta explicitamente quando o Épico 4 tratar do piso de documentação).

- source_spec: `_bmad-output/implementation-artifacts/1-2-remover-dependências-mortas-e-obsoletas.md`
  summary: **Revisão retroativa**: o pin de `maven-surefire-plugin:3.5.4` que esta story introduziu em `<pluginManagement>` do `pom.xml` raiz (corrigindo `mvn test` descobrindo 0 testes em todo o reactor) foi silenciosamente removido pela Story 1.3, ao mesmo tempo em que o `<parent><version>` do BOM externo (`scos-bom`) foi atualizado de `1.4.1` para `1.4.2` — mudança não documentada em nenhum Debug Log/File List de nenhuma das duas stories. Funciona hoje só porque o `scos-bom` 1.4.2 passou a fixar o mesmo plugin por conta própria (confirmado no repositório irmão `sawcunha-open-system-bom`).
  evidence: Confirmado por dois revisores independentes (Verification Gap e Edge Case Hunter) via `git show` dos commits das Stories 1.2/1.3 e inspeção do `pom.xml` raiz atual (sem bloco de `pluginManagement` para `maven-surefire-plugin`) e do repositório irmão (`scos-bom` 1.4.2 realmente pina `maven-surefire-plugin`). Risco real: a descoberta de testes em todo o reactor depende inteiramente de uma versão de um BOM externo continuar pinando esse plugin para sempre, sem nenhum teste/asserção neste repositório garantindo isso. Se o `scos-bom` algum dia remover esse pin (regressão do lado de fora), `mvn test` voltaria a "passar" sem rodar nada, silenciosamente, como antes da Story 1.2. Vale um teste de sanidade simples (ex.: um teste dummy que sempre falha, verificado manualmente de tempos em tempos, ou checar a versão resolvida do plugin via `mvn help:evaluate` num step de CI) para fechar esse ponto cego de forma definitiva. Encontrado pelo Verification Gap e pelo Edge Case Hunter, independentemente, durante a revisão retroativa desta story e da Story 1.3.

- source_spec: `_bmad-output/implementation-artifacts/1-2-remover-dependências-mortas-e-obsoletas.md`
  summary: **Revisão retroativa**: o pin de `reactor-core:3.8.6` no `dependencyManagement` raiz (a mais alta de duas versões conflitantes exigidas por `querydsl-jpa` vs `spring-boot-starter-data-redis`/lettuce-core) satisfaz a regra `RequireUpperBoundDeps` do enforcer, mas nunca teve sua compatibilidade binária/comportamental com os caminhos reativos do Lettuce (Redis) exercitada especificamente — só resolução de versão foi validada, não comportamento em runtime.
  evidence: Nenhum teste desta story (nem de nenhuma story posterior identificada) exercita especificamente a API reativa (Reactor-based) do Lettuce sob essa versão pinada. Mitigante: o reactor completo (incluindo `cache`/`jdempotent`, que usam Redis) já passou por builds/testes verdes em múltiplas stories subsequentes (1.10-1.15) sem nenhuma falha atribuível a essa combinação de versões — reduz bastante o risco prático, mas não o elimina formalmente. Encontrado pelo Edge Case Hunter durante a revisão retroativa desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-3-migrar-serialização-json-de-gson-para-jackson.md`
  summary: **Revisão retroativa, achado real de risco de dados**: `JsonMasker` (privacy) e `ScosAuditHashService.canonicalizeJson` (audit) usam `ObjectMapper` sem `DeserializationFeature.USE_BIG_DECIMAL_FOR_FLOATS` habilitado — números JSON não-inteiros são parseados como `double` Java (`DoubleNode`) por padrão, não como `BigDecimal`. Isso pode alterar/perder precisão numérica silenciosamente (ex.: `100.100` vira `100.1`, ou perda de dígitos significativos além de ~17) em qualquer campo numérico que passe por essas duas classes — inclusive campos NÃO relacionados a masking, já que o `JsonMasker` re-serializa a árvore inteira.
  evidence: Confirmado lendo o código de ambas as classes — nenhuma configura `USE_BIG_DECIMAL_FOR_FLOATS`/`JsonNodeFactory.withExactBigDecimals(true)`. Os testes adicionados por esta story (`1234.5678`, `42`) só usam valores que sobrevivem sem perda a um round-trip por `double`, então o gap nunca foi exercitado por teste algum. Para `ScosAuditHashService` isso é particularmente sensível: é exatamente o tipo de risco de determinismo que a Task 6 da própria story foi desenhada para mitigar (embora a determinism*idade* do hash em si provavelmente sobreviva, já que ambos os lados — escrita e verificação — usam o mesmo `ObjectMapper` com o mesmo comportamento padrão; o risco real é fidelidade dos dados originais, não inconsistência do hash-chain). Correção exigiria habilitar `USE_BIG_DECIMAL_FOR_FLOATS` nos dois `ObjectMapper`s e validar que isso não quebra nenhum outro caminho de serialização (ex.: formatação de `BigDecimal.toString()` difere de `double.toString()` em casos como notação científica) — mudança de comportamento real, não um patch trivial de uma linha; merece story própria com cobertura de teste dedicada, não uma correção apressada no fechamento do Épico 1. Encontrado pelo Edge Case Hunter durante a revisão retroativa desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-3-migrar-serialização-json-de-gson-para-jackson.md`
  summary: **Revisão retroativa**: a busca da Task 5/AC6 por classes que precisariam de tratamento especial na migração Gson→Jackson (`grep` por `TypeAdapter`/`JsonSerializer`/`JsonDeserializer`/`@JsonAdapter`) só cobre classes com adapters customizados do Gson — não cobre classes que dependiam do comportamento padrão do Gson de serializar campos privados via reflexão sem getter (Jackson exige getter ou anotação). Qualquer classe assim, fora dos 4 value objects já testados, serializaria silenciosamente como `{}` ou perderia campos sob Jackson, sem ter sido pega por essa busca.
  evidence: Metodologia da busca original confirmada lendo o Debug Log da story — cobre só os 2 padrões de customização, não o caso de reflexão implícita. Não foi possível verificar exaustivamente se existe alguma classe assim no reactor atual dentro do escopo desta revisão retroativa; registrado como ponto cego conhecido da migração, não como bug confirmado. Encontrado pelo Edge Case Hunter durante a revisão retroativa desta story.

- source_spec: `_bmad-output/implementation-artifacts/1-4-promover-o-perfil-analyze-checkstyle-archunit-para-pluginman.md`
  summary: **Revisão retroativa, achado CRÍTICO e ao vivo**: a correção no repositório irmão `sawcunha-open-system-bom` (branch `fix/1.4.3`) que esta story afirma como "aprovado e validado" — mudar o `configLocation` do Checkstyle de um artefato inexistente (`scos-build-config:1.0.0`) para um caminho local — está **apenas staged, nunca commitada** naquele repositório. O commit real da branch (`64605a8`) contém só a abertura de branch/bump de versão, não a correção em si.
  evidence: Confirmado ao vivo via `git status`/`git diff --cached pom.xml` no repositório `/home/sawcunha/Projetos/SCOS/sawcunha-open-system-bom` (branch `fix/1.4.3`): a mudança de `configLocation` está em `Mudanças a serem submetidas` (staged), não em nenhum commit. O workflow `publish-snapshot.yml` daquele repo publica snapshot automaticamente para branches `fix/*` em build verde — então qualquer publicação a partir do commit `64605a8` sozinho NÃO teria a correção, e `-Panalyze` falharia para qualquer pessoa/CI que resolva `scos-bom:1.4.3-SNAPSHOT` sem essa mesma alteração local não commitada nesta máquina. O build deste reactor (`scos-foundation`) está verde hoje só por acidente do estado local desta máquina específica. Encontrado pelo Edge Case Hunter durante a revisão retroativa desta story; **levado diretamente ao usuário para decisão**, não resolvido unilateralmente (commitar/pushear no repositório irmão é uma ação com efeito em infraestrutura compartilhada, fora do escopo de decidir sozinho).

- source_spec: `_bmad-output/implementation-artifacts/2-2-corrigir-o-handler-de-validação-para-parâmetro-simples-400.md`
  summary: `handleHandlerMethodValidationException` só chama `beanResult.getFieldErrors()` para violações de bean (`@Valid`), nunca `beanResult.getGlobalErrors()` — uma violação de nível de objeto/classe (ex.: `@AssertTrue` numa validação cross-field) seria silenciosamente descartada da resposta 400.
  evidence: Confirmado lendo o código atual e o código pré-Story-2.2 (`git show <baseline>:...`) — o padrão `.getFieldErrors()`-só já existia antes desta story, que só mudou de processar um `getBeanResults()` (o primeiro) para processar todos; não introduziu nem alterou essa lacuna. Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-2-corrigir-o-handler-de-validação-para-parâmetro-simples-400.md`
  summary: `MessageSourceResolvable.getDefaultMessage()` retornando `null` (tanto no caminho de `beanResult` quanto no de `valueResult`) pode fazer `localeService.getMessage(null, args)` lançar exceção ou resolver de forma inesperada — sem guarda contra isso em nenhum dos dois caminhos.
  evidence: Mesmo padrão de risco já existia no caminho de `beanResult`/`FieldError.getDefaultMessage()` antes desta story — não introduzido nem agravado pela mudança. `HandlerMethodValidationException` de fato pode ter ambos `getBeanResults()`/`getValueResults()` vazios simultaneamente só em teoria (Spring só lança a exceção com pelo menos um resultado registrado) — caso defensivo de baixíssima probabilidade, sem teste dedicado. Encontrado pelo Edge Case Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-2-corrigir-o-handler-de-validação-para-parâmetro-simples-400.md`
  summary: Nenhum teste de `ExceptionsHandler` neste módulo usa `MockMvc`/`@WebMvcTest` contra um `HandlerMethodValidationException` real lançado pelo Spring — todos invocam o método do handler diretamente com mocks manuais, então a forma real que o Spring produz para essa exceção nunca é verificada contra o que os testes simulam.
  evidence: Confirmado que esse é o padrão de teste já estabelecido em toda a classe `ExceptionsHandlerValidationTest`/`ExceptionsHandlerAccessDeniedTest`/etc., não uma lacuna introduzida por esta story especificamente (mesma observação já registrada para a Story 2.1). Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-3-retornar-501-para-funcionalidade-não-implementada.md`
  summary: `localeService.getMessage(code)` é chamado em 6 pontos de `ExceptionsHandler.java` (incluindo o novo handler desta story) sem nenhuma guarda contra `null`/exceção caso a chave não resolva — se `getMessage` retornar `null` ou lançar, o `ProblemDetail` fica com `detail` nulo ou o handler propaga uma exceção não tratada.
  evidence: Confirmado via `grep -n "localeService.getMessage(ScosExceptionCode"` — 6 ocorrências, 4 delas pré-existentes a esta story (`ATTRIBUTE_NOT_VALID` x2, `ACCESS_DENIED` x2), 1 pré-existente também mas modificada nesta linha de stories (`GENERIC`), e a nova desta story (`NOT_IMPLEMENTED`). É um padrão estabelecido em toda a classe, não introduzido nem agravado por esta story — mas seria uma correção de baixo risco e alto valor (um `Optional`/guarda simples) que vale endereçar de uma vez em todos os 6 pontos, não corrigir ad-hoc em um só. Encontrado pelo Edge Case Hunter durante a revisão da Story 2.3.

- source_spec: `_bmad-output/implementation-artifacts/2-3-retornar-501-para-funcionalidade-não-implementada.md`
  summary: `ScosExceptionCode.EMAIL_INVALID` (`SCOS-009`, de story anterior ao Épico 2) também não tem entrada nos bundles de mensagem `core/src/main/resources/scos_utils_messages{,_en}.properties` — mesma lacuna que `SCOS-010` tinha (corrigida nesta story, só para o código novo).
  evidence: Confirmado via `grep -c "SCOS-009"` retornando 0 nos dois arquivos. Fora do escopo da Story 2.3 corrigir um código que não é seu. Encontrado pelo Verification Gap durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-3-retornar-501-para-funcionalidade-não-implementada.md`
  summary: `MethodNotImplementedException` continua sendo um `RuntimeException` puro, sem `ExceptionCode`/`getHttpCode()` — o `HttpStatus.NOT_IMPLEMENTED` está hardcoded no handler em vez de vir de um `getHttpCode()` de enum, ao contrário do padrão `ScosException`/`ScosNoRollbackException` que as outras 9 exceções seguem.
  evidence: Decisão deliberada e explícita da própria story (Dev Notes: "mudar a hierarquia dessa exceção está fora do escopo desta story") — não é uma omissão, é uma escolha de escopo documentada. Registrado aqui só para não se perder como possível trabalho futuro (unificar `MethodNotImplementedException` ao padrão `ScosException`), caso uma story futura precise desse `getHttpCode()` dinâmico. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-4-padronizar-log-por-faixa-de-status-http.md`
  summary: `logByStatus(HttpStatus, String, Throwable)` roteia qualquer status que não seja `4xx` para o ramo `ERROR`/com-stack-trace — incluindo um hipotético `1xx`/`2xx`/`3xx`, caso algum `ExceptionCode` algum dia retorne um `httpCode` fora da faixa de erro. Um código assim seria logado como falha grave mesmo representando uma resposta não-erro.
  evidence: Nenhum `ScosExceptionCode` de produção usa uma faixa `1xx`/`2xx`/`3xx` hoje (todos são `4xx`, exceto `NOT_IMPLEMENTED`, que é tratado por outro handler fixo, não por `logByStatus`), e `ExceptionCode.getHttpCode()` tem `default` 400 — cenário sem caminho de produção conhecido hoje. Redesenhar `logByStatus` para uma terceira faixa (`is1xxInformational`/`is2xxSuccessful`/`is3xxRedirection`) não foi pedido pela AC desta story, que só exige diferenciar `4xx`/`5xx`. Encontrado pelo Edge Case Hunter durante a revisão desta story; registrado para não se perder caso um `ExceptionCode` futuro use uma faixa não-erro.

- source_spec: `_bmad-output/implementation-artifacts/2-5-corrigir-a-resolução-de-título-de-erro-para-jackson-3.md`
  summary: `_bmad-output/planning-artifacts/epics.md`/`prd.md` (FR14) ainda descrevem a correção do Épico 2/Story 2.5 como sendo no método `resolveTitle`, mas o Task 0 desta própria story confirmou (e a implementação seguiu) que o alvo real é `handleHttpMessageNotReadable` — `resolveTitle` não usa regex nenhum, é só um `try/catch` simples, e é o alvo de uma story diferente (2.6).
  evidence: Confirmado lendo o código atual de `ExceptionsHandler.resolveTitle` (só `try { localeService.getMessage(title) } catch { "Business Error" }`, sem `Pattern`/`Matcher`) e comparando com `epics.md#story-25-corrigir-a-resolução-de-título-de-erro-para-jackson-3`/FR14, que ainda nomeiam `resolveTitle` como alvo. Corrigir os documentos de planejamento está fora do escopo de uma story de bug-fix pontual — registrado para não induzir uma leitura futura errada da FR14. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-5-corrigir-a-resolução-de-título-de-erro-para-jackson-3.md`
  summary: `handleHttpMessageNotReadable` resolve a mensagem de `detail` via `localeService.getMessage(ScosExceptionCode.ATTRIBUTE_NOT_VALID.getCode(), field, typesEnum)` (código `SCOS-001`), mas o bundle de mensagens (`core/src/main/resources/scos_utils_messages{,_en}.properties`) não tem placeholders `{0}`/`{1}` em `SCOS-001` — quem tem os placeholders é `SCOS-002`/`ENUM_ERROR`, usado 3 linhas abaixo só para `type`/`title` do `ProblemDetail`, não para `detail`. Mesmo com `field`/`typesEnum` agora extraídos corretamente por esta story (Task 2), eles nunca aparecem no texto de `detail` de uma resposta real com um `LocaleService` real, porque são passados para o template errado.
  evidence: Confirmado lendo os dois arquivos de bundle — `SCOS-001` é texto fixo ("Os atributos informados não correspondem ao esperado..."), `SCOS-002` tem `''{0}''`/`{1}` ("Os tipos permitidos para ''{0}'' de campo são os seguintes: {1}."). A linha do `localeService.getMessage(...)` em si não foi tocada por esta story (só os valores de `field`/`typesEnum` que ela recebe mudaram) — bug pré-existente, não causado nem agravado aqui. Corrigir exigiria trocar `ATTRIBUTE_NOT_VALID`/`SCOS-001` por `ENUM_ERROR`/`SCOS-002` na chamada de `detail`, mudança de comportamento de resposta HTTP fora do escopo desta story (que é só sobre COMO `field`/`typesEnum` são extraídos, não sobre qual código de mensagem é usado). Encontrado pelo Verification Gap Reviewer durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-6-corrigir-resolvetitle-para-não-usar-try-catch-como-controle.md`
  summary: `resolveTitle` usa o próprio texto do título em inglês já resolvido (ex.: `"Validation Error"`, `"Access Denied"`) como CHAVE de busca no `LocaleService`, não um código estável como `SCOS-001`. Nenhum bundle de mensagens deste repositório (`core/src/main/resources/scos_utils_messages{,_en}.properties`) tem uma entrada cuja chave seja esse texto literal — então o ramo de "tradução" nunca resolve de fato contra os bundles deste repositório, e o título retornado é sempre o mesmo texto em inglês passado como default, minando o objetivo declarado da própria story ("não ver um literal em inglês numa API em PT-BR").
  evidence: **Não é um bug introduzido por esta story** — confirmado comparando com o código anterior (`baseline_commit` desta story): `resolveTitle` já chamava `localeService.getMessage(title)` (mesma chave, o texto do título) antes da Story 2.6 tocar no arquivo; esta story só trocou o mecanismo de fallback (try/catch com literal fixo `"Business Error"` → `getMessageOrDefault` com o próprio `title` como default), não a chave de lookup. `ScosExceptionCode.getTitle()` retorna textos fixos em inglês ("Validation Error", "Access Denied", "Unauthorized", "Not Implemented", "Internal Server Error") — confirmado lendo o enum. Corrigir isso exigiria decidir uma convenção nova de chave estável para título (ex.: `SCOS-001.title`) e popular os bundles com ela — escopo maior que uma correção pontual, e as Dev Notes desta própria story pedem explicitamente para NÃO introduzir mecanismos novos de resolução de mensagem. Encontrado pelo Blind Hunter durante a revisão desta story; verificado e confirmado pré-existente por este orquestrador antes de registrar aqui (não geraria loopback de intent_gap/bad_spec por não ser causado por esta story).

- source_spec: `_bmad-output/implementation-artifacts/2-6-corrigir-resolvetitle-para-não-usar-try-catch-como-controle.md`
  summary: `LocaleUtilsBean.getMessageOrDefault` — a única implementação do novo método de contrato `LocaleService.getMessageOrDefault` em todo o repositório — nunca é executada por nenhum teste, direto ou indireto. Todo teste que alcança `resolveTitle` usa um `LocaleService` mockado; nenhum contexto Spring do repositório conecta `LocaleUtilsBean` a `ExceptionsHandler`.
  evidence: Confirmado via `grep -rn "getMessageOrDefault" audit/` — só a própria declaração do método, nenhuma chamada. `LocaleUtilsBean` nunca teve arquivo de teste dedicado (confirmado via `git log --follow` — é um fixture de suporte em `src/test`, usado indiretamente por outros testes do módulo `audit`, nunca testado diretamente), então a ausência de teste para este método específico segue o padrão já existente da classe — mas é o único trecho de lógica nova desta story (o mapeamento de argumentos da sobrecarga de 4 argumentos do Spring `MessageSource.getMessage`) sem nenhuma execução real em todo o build. Encontrado pelo Verification Gap Reviewer e pelo Blind Hunter, independentemente, durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-8-mover-o-contrato-de-domínio-do-exception-para-o-core.md`
  summary: `ScosSecurityException` (movida de `exception` para `core.exception` nesta story) não é referenciada em nenhum outro arquivo do reactor além da sua própria declaração — código morto, movido mas nunca usado, sem nenhum teste dedicado antes ou depois do mover.
  evidence: Confirmado via `grep -rn "ScosSecurityException" --include="*.java" .` (fora de `/target/`) — só a própria classe. Decisão sobre removê-la (biblioteca "Foundation" pode ter consumidores externos que a usam sem que apareça no grep deste repo) ou mantê-la fica fora do escopo de uma story de "mover sem mudar comportamento" (AC #3). Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/2-8-mover-o-contrato-de-domínio-do-exception-para-o-core.md`
  summary: as 4 classes movidas para `core.exception` (`ScosNoContentException`, `ScosNoRollbackException`, `ScosSecurityException`, `MethodNotImplementedException`) têm superfícies de construtor inconsistentes entre si (`MethodNotImplementedException` tem 5 sobrecargas incluindo uma `protected`; as outras têm 1-2) e `ScosSecurityException` especificamente tem 2 construtores que não delegam um ao outro (duplicação de `super(...)`). Indentação também é inconsistente entre as 4 (tabs vs. 4 espaços).
  evidence: Confirmado via `diff` do conteúdo antigo (`exception/.../error/`) vs. novo (`core/.../exception/`), ignorando a linha `package` — as inconsistências já existiam antes desta story, que só mudou o pacote (e removeu 1 import redundante em 3 delas). Corrigir estilo/API de construtor está fora do escopo da Task 4 desta story, que exige explicitamente um commit de "mover" sem tocar em lógica/formatação. Encontrado pelo Blind Hunter durante a revisão desta story.

- source_spec: `_bmad-output/implementation-artifacts/epic-2-retro-2026-08-29.md`
  summary: **Retrospectiva do Épico 2 — lição de processo recorrente**: pela segunda vez consecutiva (Épico 1 → Épico 2), uma story que remove/move um módulo inteiro (Story 2.9, remoção de `exception` do reactor) fechou sem grep por referências residuais fora do código-fonte — README.md, javadocs de outras classes, `.github/workflows/build.yml`. Já corrigido pela retro: seção "SCOS Foundation Exception" do `README.md` (renomeada para "SCOS Foundation Web"), javadoc de `ExceptionsHandlerHandleExceptionInternalTest.java` e de `ValidationAnnotationCountListener.java`, e os 2 steps mortos de CI (`Archive`/`Publish exception module artifacts`) removidos de `build.yml`.
  evidence: Mesmo padrão já registrado acima para a Story 1.14 (`utils`). **Não corrigido nesta passada** (fora do escopo estreito da retro do Épico 2, mesma razão já registrada para o gap de `utils`): a skill de configuração dedicada `etc/doc/skills/scos-exception-config/SKILL.md` (e sua linha em `etc/doc/skills/README.md`) continua descrevendo um módulo que não existe mais, ativado por component-scan — quando na verdade a ativação de erro HTTP agora é via auto-configuração do módulo `web`. Fica como debt para quando o Épico 4 (piso de documentação) tratar do reescrito geral do `README.md`/skills, junto com o gap já registrado de `utils`. **Recomendação de processo**: tornar obrigatório, como Acceptance Criteria de qualquer story futura de "mover/remover módulo", um grep pelo nome do módulo removido em `README.md`, `.github/workflows/*.yml`, `etc/doc/skills/**` e comentários javadoc do reactor inteiro antes de marcar a story como `done` — em vez de descobrir isso só na retrospectiva do épico seguinte.

- source_spec: `_bmad-output/implementation-artifacts/3-1-corrigir-a-inicialização-do-redisconnectionfactory.md`
  summary: `ScosCacheConfiguration.scosLettuceConnectionFactory()` (`cache/src/main/java/br/com/sawcunhaos/foundation/cache/ScosCacheConfiguration.java:84`) tem o mesmo bug de NPE incondicional em `redisProperties.getSentinel().getMaster()` que esta story corrigiu no `jdempotent`, e continua sem correção.
  evidence: Achado pelo Verification Gap Reviewer durante a revisão desta story — grep confirma que nenhum teste no repo cobre `ScosCacheConfiguration`/`scosLettuceConnectionFactory` (o módulo `cache` não tem sequer diretório de teste), então qualquer app consumidora com `spring.cache.enabled` (default `true`) e Redis standalone/cluster sem bloco `sentinel:` quebra no boot com o mesmo NPE que a Story 3.1 eliminou no `jdempotent`. Fora do escopo desta story (File List restrito a `ScosJdempotentRedisConfiguration.java`).

- source_spec: `_bmad-output/implementation-artifacts/3-1-corrigir-a-inicialização-do-redisconnectionfactory.md`
  summary: `clusterConfiguration()` (novo, introduzido por esta story) não valida se `redisProperties.getCluster().getNodes()` está vazio antes de montar `RedisClusterConfiguration`, podendo gerar falha opaca de conexão em vez de erro claro de configuração.
  evidence: Achado pelo Blind Hunter e pelo Edge Case Hunter, de forma independente, durante a revisão desta story. Fora do escopo do AC #1 (que exige apenas ausência de NPE), então não bloqueou esta story.

- source_spec: `_bmad-output/implementation-artifacts/3-1-corrigir-a-inicialização-do-redisconnectionfactory.md`
  summary: `redisConfiguration()` (novo, introduzido por esta story) não documenta nem alerta quando `spring.data.redis.sentinel.*` e `spring.data.redis.cluster.*` estão configurados simultaneamente — a precedência (sentinel > cluster > standalone) escolhe silenciosamente o Sentinel, ignorando o Cluster configurado.
  evidence: Achado pelo Blind Hunter e pelo Edge Case Hunter, de forma independente, durante a revisão desta story. A Task 3 desta story pediu cobertura dos três cenários de topologia isolados, não do caso ambíguo de configuração dupla — fora do escopo do AC #1.

- source_spec: `_bmad-output/implementation-artifacts/3-1-corrigir-a-inicialização-do-redisconnectionfactory.md`
  summary: Nenhuma das três topologias (`sentinelConfiguration()`, `clusterConfiguration()`, `standaloneConfiguration()`) em `ScosJdempotentRedisConfiguration` conecta `redisProperties.getSsl()` ao `LettuceClientConfiguration` — deployments de cluster/sentinel em produção comumente exigem TLS.
  evidence: Achado pelo Blind Hunter durante a revisão desta story. Comportamento pré-existente (o `LettuceClientConfiguration` já não tratava SSL antes desta story) e explicitamente fora do escopo por NFR4 (Dev Notes da story restringem a mudança à fonte de topologia, preservando a configuração de resiliência do Lettuce).

- source_spec: `_bmad-output/implementation-artifacts/3-2-migrar-hashing-de-md5-para-sha-256.md`
  summary: Chaves de idempotência já persistidas (ex.: Redis) sob MD5 não batem com as novas chaves SHA-256 geradas após o deploy desta mudança, desabilitando silenciosamente a proteção de deduplicação durante a janela de rollout.
  evidence: Achado pelo Blind Hunter e pelo Edge Case Hunter, de forma independente, durante a revisão desta story. As Dev Notes desta story descartam explicitamente introduzir seleção dinâmica/versionamento de algoritmo ("escopo especulativo não pedido") e o NFR4 restringe a mudança a uma troca mecânica de algoritmo/formatação — a estratégia de rollout seguro (versionar a chave, aceitar a janela por causa do TTL, comunicar o deploy) é uma decisão de produto/arquitetura fora do escopo desta story pontual.

- source_spec: `_bmad-output/implementation-artifacts/3-3-resolver-campos-anotados-em-toda-a-hierarquia-de-classes.md`
  summary: `IdempotentAspect.setJdempotentId()` (linha ~283) continua usando `arg.getClass().getDeclaredFields()` sem percorrer a hierarquia — um campo anotado `@JdempotentId` declarado numa superclasse nunca recebe de volta a chave gerada, falhando silenciosamente (nenhuma exceção, o campo simplesmente fica sem o valor).
  evidence: Achado pelo Blind Hunter e pelo Verification Gap Reviewer, de forma independente, durante a revisão desta story. A própria Task 2 desta story já documentava essa lacuna explicitamente e condicionava a extensão do escopo a confirmação do epics.md ou do dono do requisito, confirmação que não ocorreu nesta execução — decisão consciente de escopo, não uma omissão desta revisão. Nenhum teste do repositório cobre `setJdempotentId`/`execute()` com um payload cujo campo `@JdempotentId` esteja numa superclasse.

- source_spec: `_bmad-output/implementation-artifacts/3-3-resolver-campos-anotados-em-toda-a-hierarquia-de-classes.md`
  summary: Em `IdempotentAspect.getIdempotentNonIgnorableWrapper`, um `arg` nulo dentro da lista de argumentos causa `NullPointerException` em `arg.getClass()` (ou em `isTypePrimitive(arg)`, dependendo da implementação), sem nenhuma guarda contra `null`.
  evidence: Achado pelo Edge Case Hunter durante a revisão desta story. Comportamento pré-existente — o código anterior a esta story (`Field[] declaredFields = arg.getClass().getDeclaredFields();`) tinha exatamente o mesmo risco de NPE para `arg == null`; esta story não introduziu nem agravou o problema, só trocou a fonte dos campos por `getAllFieldsInHierarchy`.

- source_spec: `_bmad-output/implementation-artifacts/3-4-registrar-idempotentaspect-sem-duplicidade-e-sem-threadlocal.md`
  summary: A correção de duplicidade de bean em `ScosJdempotentConfig` depende da ordem de declaração dos métodos `@Bean` na classe (`@ConditionalOnMissingBean` só funciona porque `getIdempotentAspectOnErrorConditionalCallback()` está declarado antes de `getIdempotentAspect()`), o que é frágil e não é garantido pelo contrato do Spring caso os métodos sejam reordenados no futuro.
  evidence: Confirmado nas Completion Notes da própria story (Task 1): "Spring processa métodos @Bean de uma mesma classe @Configuration/@AutoConfiguration na ordem de declaração no source... Se a ordem fosse invertida, @ConditionalOnMissingBean seria avaliado primeiro... e criaria o bean default de qualquer forma, resultando de novo em dois beans." O AC exigia apenas resolver a duplicidade confirmada (não redesenhar a configuração), então a story documentou o risco em vez de eliminá-lo via `ObjectProvider<ErrorConditionalCallback>` ou classes de configuração separadas — abordagem mais robusta a considerar numa story futura de hardening.

- source_spec: `_bmad-output/implementation-artifacts/3-4-registrar-idempotentaspect-sem-duplicidade-e-sem-threadlocal.md`
  summary: `getIdempotentAspectOnErrorConditionalCallback()` em `ScosJdempotentConfig.java` não tem `@ConditionalOnMissingBean(IdempotentAspect.class)` — se uma aplicação consumidora registrar seu próprio bean `IdempotentAspect` customizado E também registrar um `ErrorConditionalCallback`, o Spring ainda cria um segundo bean `IdempotentAspect` (o mesmo tipo de conflito que esta story corrigiu para o par de métodos internos da própria lib).
  evidence: Achado pela revisão edge-case da Story 3.4. Pré-existente ao fix desta story — antes da mudança, nenhum dos dois métodos tinha guarda, então esse cenário (bean customizado do consumidor + `ErrorConditionalCallback`) já produzia conflito antes; não foi causado nem agravado por esta story, que tratou apenas a duplicidade confirmada entre os dois métodos internos (`Bug real confirmado` na story). Resolve-se aplicando `@ConditionalOnMissingBean(IdempotentAspect.class)` também em `getIdempotentAspectOnErrorConditionalCallback()`.

- source_spec: `_bmad-output/implementation-artifacts/3-4-registrar-idempotentaspect-sem-duplicidade-e-sem-threadlocal.md`
  summary: `getIdempotentAspectOnErrorConditionalCallback()` repete no nível do método o mesmo `@ConditionalOnProperty(prefix="scos.jdempotent", name="enabled", ...)` já presente no nível da classe `ScosJdempotentConfig`, uma duplicação redundante pré-existente não relacionada ao bug desta story.
  evidence: Achado pela revisão blind-hunter da Story 3.4. Pré-existente à mudança (não introduzido por ela) e fora do escopo do AC (que trata apenas de `@ConditionalOnMissingBean` e do `ThreadLocal`); por NFR2 desta mesma story, misturar essa limpeza não relacionada no mesmo commit seria indesejado.

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: Em `RedisIdempotentRepository`, o `payloadHash` gravado por `tryAcquire` não sobrevive à chamada subsequente de `setResponse()` — essa reconstrói o wrapper via um overload de `prepareValue(request, response)` pré-existente que nunca seta `payloadHash`, então uma vez que a resposta é gravada o hash do payload original se perde.
  evidence: Confirmado por leitura direta do código (`RedisIdempotentRepository.java`, overloads de `prepareValue`) e já documentado nas próprias Completion Notes da story como risco conhecido para a Story 3.6 (colisão de payload) resolver — o AC desta story só exigia threading do parâmetro em `tryAcquire`, não persistência ponta a ponta.

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: `Duration.of(customTtl, timeUnit.toChronoUnit())` em `IdempotentAspect.execute()` pode lançar `ArithmeticException` para combinações extremas de `ttl()`/`ttlTimeUnit()` na anotação `@JdempotentResource` (ex.: valor próximo de `Long.MAX_VALUE` com `TimeUnit.DAYS`) — antes desta story, `customTtl`/`timeUnit` eram passados como `Long`+`TimeUnit` crus para `store()`, sem essa conversão, então o overflow é uma nova via de falha introduzida pela mudança.
  evidence: Achado pelo Edge Case Hunter durante a revisão desta story. Requer uma configuração de TTL absurda/não-realista na anotação para disparar; nenhum AC desta story pede validação de bounds do TTL configurado.

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: Em `RedisIdempotentRepository.tryAcquire`, o `GET` de fallback usado para popular `existingPayloadHash`/`existingResponse` quando o `SET NX` falha não é atômico com esse `SET` — se o lease do dono expirar exatamente nesse intervalo, o chamador recebe `IdempotentInProgressException` (409) mesmo sem ninguém mais segurando o lock.
  evidence: Achado de forma independente pelo Blind Hunter e pelo Edge Case Hunter durante a revisão desta story. Janela estreita (poucos milissegundos), efeito transitório que se autocorrige numa nova tentativa do chamador; já documentado no Javadoc do método como limitação aceita para a garantia de exclusividade do lock (foco do AC #1).

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: `RedisIdempotentRepository.tryAcquire` sempre grava `request=null` no placeholder (via `prepareValue(null, payloadHash)`), então mesmo com `redisProperties.getPersistReqRes()=true` o request real fica ausente do registro em Redis durante toda a janela em que a chamada está em andamento (só aparece depois, se/quando `setResponse()` gravar com sucesso) — a property `persistReqRes` não é honrada nesse estado intermediário.
  evidence: Confirmado por leitura direta do código. Corrigir exigiria acrescentar o `request` real à assinatura de `tryAcquire` (`IdempotentRepository.tryAcquire(IdempotencyKey, String, Duration)`), mas essa assinatura de 3 parâmetros é a que a Task 2 desta story pede explicitamente — mudá-la agora seria escopo além do pedido; revisitar se uma story futura já precisar tocar essa assinatura por outro motivo.

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: `payloadHash` é calculado em `IdempotentAspect.execute()` a partir de `requestObject.toString()` — para payloads cuja classe não sobrescreve `toString()` de forma baseada em conteúdo, o hash não reflete de fato o conteúdo do payload, esvaziando a comparação de colisão que a Story 3.6 pretende construir em cima deste campo.
  evidence: Achado pelo Blind Hunter durante a revisão desta story. Limitação pré-existente do esquema de geração de chave já reaproveitado (o mesmo padrão que `DefaultKeyGenerator`/`generateIdempotentKey` já usava antes desta story para compor a `IdempotencyKey`); já sinalizada nas próprias Completion Notes da story como algo a considerar na Story 3.6.

- source_spec: `_bmad-output/implementation-artifacts/3-18-qualidade-de-teste-do-jdempotent-redis.md`
  summary: Story secundária carve-off — cobertura parametrizada das 3 topologias Redis (Standalone/Sentinel/Cluster) com concorrência real via Testcontainers (AD-7) e o teste de throughput/TPS informativo (AD-9), ambos previstos na spine de arquitetura `architecture-SawCunhaOS-Foundation-2026-08-29`.
  evidence: O spec original da 3.18 cobrindo as 4 frentes (split unit/IT, topologia, fail-fast, TPS) somava ~3015 tokens (limite recomendado do workflow: 1600), disparando o gate de token count do step-02. Decisão do usuário (2026-08-29): dividir em duas stories — 3.18 mantém a fundação (split unit/IT via Failsafe, ajuste de CI, renomeação dos testes sem sufixo IT, fail-fast em config inválida); a topologia parametrizada e o TPS ficam para uma story seguinte, que também depende tecnicamente da infraestrutura Failsafe da 3.18 já existir (os novos `*ITTest` precisam do plugin/CI já wireados para rodar).

- source_spec: `_bmad-output/implementation-artifacts/3-5-tornar-a-aquisição-do-lock-atômica-tryacquire-lease.md`
  summary: Se o método protegido por `@JdempotentResource` lançar um `Throwable` que não seja `Exception` (ex.: `Error`/`OutOfMemoryError`/`StackOverflowError`), o `catch (Exception e)` em `IdempotentAspect.execute()` não libera o lease — com o novo fluxo `tryAcquire`, isso trava a chave permanentemente (toda chamada futura recebe `409 IN_PROGRESS` até o TTL expirar no Redis, ou para sempre no `InMemoryIdempotentRepository`, que não impõe TTL), uma consequência mais severa do que o comportamento antigo (que apenas retornava `null` silenciosamente em chamadas subsequentes).
  evidence: Achado pelo Edge Case Hunter durante a revisão desta story. O `catch (Exception e)` em si é código pré-existente, não tocado pelo diff desta story (fora do File List) — a mudança apenas tornou a consequência de um `Throwable` não capturado mais severa (lockout permanente vs. `null` silencioso); corrigir exigiria editar uma linha fora do escopo de arquivos desta story.

- source_spec: `_bmad-output/implementation-artifacts/3-18-qualidade-de-teste-do-jdempotent-redis.md`
  summary: O job `security-check` do CI (`.github/workflows/build.yml`, `mvn -B -Panalyze test`) nunca roda `verify`/Failsafe — após o novo `<excludes>**/*ITTest.java</excludes>` no Surefire, os 3 testes `*ITTest` do `jdempotent` (incluindo `RedisIdempotentRepositoryTryAcquireITTest` da Story 3.5) deixam de rodar nesse job, encolhendo silenciosamente o conjunto de testes que alimenta o relatório JaCoCo desse job específico. O `jacoco-maven-plugin` do perfil `analyze` também só tem a execução `prepare-agent` (Surefire), sem `prepare-agent-integration`/`report-integration` para o Failsafe — testes de integração não contribuem para a cobertura JaCoCo em nenhum job.
  evidence: Achado de forma independente pelo Blind Hunter e pelo Verification Gap Reviewer durante a revisão desta story. Corrigir exigiria mudar o `jacoco-maven-plugin` do perfil `analyze` no POM raiz e/ou adicionar `verify` ao job `security-check` — fora do escopo autorizado desta story (Code Map/Boundaries listam só `jdempotent/pom.xml` e `.github/workflows/build.yml` linha 44-45).

- source_spec: `_bmad-output/implementation-artifacts/3-18-qualidade-de-teste-do-jdempotent-redis.md`
  summary: `PrimeNumbersJdempotentEnableITTest`/`DisableITTest` usam `src/test/resources/docker-compose.yml` com portas de host fixas (`6379`/`26379`) e um `@Container` de instância (não `static`) — o Testcontainers sobe/derruba um `ComposeContainer` novo por método de teste, e o Docker não libera a porta a tempo entre teardown e o próximo `compose up -d`, causando `port is already allocated`/timeout. Isso faz `mvn verify` no `jdempotent` falhar hoje, mesmo com o wiring do Failsafe (AD-6) correto.
  evidence: Confirmado pré-existente pelo Dev Agent Record da Story 3.18: reproduzido de forma idêntica no `baseline_commit` (antes de qualquer mudança da story), rodando só `PrimeNumbersJdempotentEnableTest` sozinho — os 2 métodos da mesma classe já colidiam entre si. Mesmo root cause já documentado nos Dev Notes das Stories 3.1/3.4/3.5. `RedisIdempotentRepositoryTryAcquireITTest` (Story 3.5) não é afetado — usa `GenericContainer` `static` com porta dinâmica, exatamente para evitar esse problema. Corrigir exigiria mudar a fixture (`docker-compose.yml` para portas dinâmicas e/ou tornar o `@Container` `static`/compartilhado por classe) — fora do escopo autorizado pela Story 3.18 ("renomear sem mudar lógica"). Decisão do usuário (2026-08-29): registrar aqui para uma story futura de hardening do fixture Docker Compose, em vez de expandir o escopo da 3.18.

- source_spec: `_bmad-output/implementation-artifacts/3-19-topologia-redis-parametrizada-concorrencia-e-throughput.md`
  summary: `findFreePort()`/`findFreeConsecutivePorts()` em `RedisIdempotentRepositoryTopologyITTest` têm uma janela clássica de TOCTOU — a porta é sondada com um `ServerSocket` que é fechado antes de o container Docker efetivamente fazer o bind, então outro processo pode ocupar a porta nesse intervalo.
  evidence: Sinalizado de forma independente pelos 3 reviewers da Story 3.19 (blind-hunter, edge-case-hunter e verification-gap). Corrigir de verdade exige um retry-on-bind-failure em torno da criação do container (não é um patch trivial de 1-2 linhas); mitigação real fica para quando isso causar flakiness observável em CI.

- source_spec: `_bmad-output/implementation-artifacts/3-19-topologia-redis-parametrizada-concorrencia-e-throughput.md`
  summary: As 3 imagens Redis usadas em `RedisIdempotentRepositoryTopologyITTest` (`bitnami/redis:latest`, `bitnami/redis-sentinel:latest`, `grokzen/redis-cluster:latest`) estão fixadas em `:latest` — um update upstream pode quebrar silenciosamente os testes (ex.: as próprias suposições de `protected-mode`/template documentadas no código para o Cluster).
  evidence: Sinalizado pelo blind-hunter durante a revisão da Story 3.19. Padrão pré-existente (o teste da Story 3.5 que foi migrado já usava `:latest`), mas agora com 3 imagens em vez de 1 o raio de exposição a esse risco triplicou. Fixar versões exatas é mudança de baixo risco mas não crítica; fica para quando a flakiness de update de imagem for observada.

- source_spec: `_bmad-output/implementation-artifacts/3-6-detectar-colisão-de-payload-sob-a-mesma-chave.md`
  summary: `RedisIdempotentRepository.setResponse()` chama `requestResponseWrapper.setResponse(response)` mas essa mutação no objeto local nunca é persistida — o valor realmente gravado é `newValue`, construído separadamente via `prepareValue(request, response)`; a chamada é código morto e só atrapalha a leitura do método.
  evidence: Achado pelo Blind Hunter durante a revisão desta story. Linha de contexto pré-existente no diff (não introduzida pela Story 3.6) — o `setResponse()` já ignorava essa mutação antes desta story; a correção é remover a linha, mas está fora do File List/escopo autorizado desta story.

- source_spec: `_bmad-output/implementation-artifacts/3-6-detectar-colisão-de-payload-sob-a-mesma-chave.md`
  summary: `RedisIdempotentRepository.setResponse()` faz `contains(idempotencyKey)` e depois `valueOperations.get(...)` como dois round-trips separados ao Redis — se a chave expirar/for removida entre as duas chamadas, `requestResponseWrapper` vem `null` e a linha seguinte (`.setResponse(...)`/`.getPayloadHash()`) lança NPE, silenciosamente engolida pelo `catch (Exception e)` genérico do método: a resposta não é persistida e nenhum erro chega ao chamador.
  evidence: Achado pelo Blind Hunter durante a revisão desta story. Race pré-existente (não introduzida pela Story 3.6), mas a Story 3.6 passou a depender desse mesmo `requestResponseWrapper` não-nulo para repropagar o `payloadHash`; nenhum teste cobre o caso `null`. Corrigir exigiria uma leitura atômica (`GETSET`/script Lua) fora do escopo desta story.

- source_spec: `_bmad-output/implementation-artifacts/3-6-detectar-colisão-de-payload-sob-a-mesma-chave.md`
  summary: Tanto `AbstractIdempotentRepository.tryAcquire` quanto `RedisIdempotentRepository.tryAcquire` só sinalizam `Lease.mismatch(...)` quando `existingPayloadHash != null && !existingPayloadHash.equals(payloadHash)` — quando o hash já armazenado é `null` (chave herdada de antes da Story 3.5/3.6, ou gravada por um chamador que não usa `IdempotentAspect`), a comparação é pulada silenciosamente e a chamada cai no fluxo antigo (`inProgress`/cache), sem sinalizar a colisão de payload que as ACs #1/#2 pedem.
  evidence: Achado de forma independente pelo Blind Hunter e pelo Edge Case Hunter durante a revisão desta story. Não é alcançável hoje pelo único chamador real (`IdempotentAspect` sempre calcula um `payloadHash` não-nulo antes de chamar `tryAcquire`), mas ambos os métodos de repositório são API pública do módulo `jdempotent`; decidir o comportamento correto para hash desconhecido (tratar como mismatch por segurança vs. manter o fallback permissivo atual) é uma decisão de design não coberta pelo texto da story.

- source_spec: `_bmad-output/implementation-artifacts/3-6-detectar-colisão-de-payload-sob-a-mesma-chave.md`
  summary: `IdempotentResponseWrapper.equals(Object obj)` compara `this.response.equals(obj)` (o campo interno contra o objeto `obj` inteiro) em vez de `obj` convertido para `IdempotentResponseWrapper` e comparar os campos — a implementação está estruturalmente quebrada e praticamente nunca retorna `true` para duas instâncias com o mesmo `response`.
  evidence: Achado incidentalmente pelo subagente de implementação ao escrever um teste desta story que comparava dois `IdempotentResponseWrapper` com `assertEquals` — o teste falhou e foi reescrito para `assertSame` para não depender do `equals()` quebrado. Bug pré-existente, não introduzido pela Story 3.6, e fora do File List/escopo autorizado; código em `jdempotent/src/main/java/br/com/sawcunhaos/foundation/jdempotent/core/model/IdempotentResponseWrapper.java` (método `equals`). **Corrigido pela Story 3.7** (decisão explícita do usuário de manter a correção do `equals()` dentro do escopo dessa story).

- source_spec: `_bmad-output/implementation-artifacts/3-7-garantir-fail-open-com-circuit-breaker-quando-o-redis-está-i.md`
  summary: `resilience4j-spring-boot4` foi declarado `optional=true` em `jdempotent/pom.xml` (conforme ADD-3, "não deve vazar como dependência transitiva obrigatória"), mas `RedisIdempotentRepository` referencia `CircuitBreaker`/`CircuitBreakerConfig` incondicionalmente no construtor e em toda operação Redis — é, na prática, uma dependência obrigatória do módulo, não opcional.
  evidence: Achado independentemente pelo Blind Hunter e pelo Verification Gap Reviewer. Uma aplicação consumidora que dependa de `scos-foundation-jdempotent` para o backend Redis sem declarar `resilience4j-spring-boot4` explicitamente sofre `NoClassDefFoundError` em runtime ao primeiro `new RedisIdempotentRepository(...)`. É uma decisão de arquitetura (ADD-3) — exige documentar a exigência para consumidores (README) ou revisar se `optional=true` é a semântica correta aqui — não um bug de código corrigível sem essa decisão.

- source_spec: `_bmad-output/implementation-artifacts/3-7-garantir-fail-open-com-circuit-breaker-quando-o-redis-está-i.md`
  summary: O novo circuit breaker em `RedisIdempotentRepository` não emite nenhum evento/log/métrica nas transições de estado (`CLOSED→OPEN→HALF_OPEN`) — hoje a única forma de perceber que o módulo caiu em fail-open é grepar os `log.error` por chamada individual.
  evidence: Achado pelo Blind Hunter. Provavelmente sobrepõe o escopo da Story 3.11 ("expor métricas de idempotência"), que ainda está `ready-for-dev` — decidir se a observabilidade do circuit breaker entra nessa story ou é tratada aqui é uma decisão de escopo, não uma correção de código isolada.

- source_spec: `_bmad-output/implementation-artifacts/3-7-garantir-fail-open-com-circuit-breaker-quando-o-redis-está-i.md`
  summary: `CircuitBreakerConfig` do novo breaker não configura `recordExceptions`/`ignoreExceptions` — qualquer exceção lançada dentro das operações protegidas (incluindo um bug de programação, não só falha real do Redis) conta para abrir o circuito.
  evidence: Achado pelo Blind Hunter. Sem filtragem, um erro de código (ex.: o NPE de `getResponse()` em cache miss, corrigido nesta story) teria contado erroneamente como sinal de indisponibilidade do Redis antes da correção. Decidir a lista de exceções "reais" de infraestrutura Redis vs. erros de programação é uma decisão de design não coberta pelo texto da story.

- source_spec: `_bmad-output/implementation-artifacts/3-7-garantir-fail-open-com-circuit-breaker-quando-o-redis-está-i.md`
  summary: `RedisIdempotentRepository.DEFAULT_SLOW_CALL_THRESHOLD` (5s, fallback do circuit breaker) duplica o `commandTimeout` hardcoded separadamente em `ScosJdempotentRedisConfiguration` — nada mantém os dois sincronizados; se um mudar, o outro pode divergir silenciosamente.
  evidence: Já documentado pelo próprio subagente de implementação nas Completion Notes da Story 3.7 como gap pré-existente e fora do escopo declarado (só `pom.xml` + `RedisIdempotentRepository.java`). Confirmado de forma independente pelo Blind Hunter na revisão.

- source_spec: `_bmad-output/implementation-artifacts/3-7-garantir-fail-open-com-circuit-breaker-quando-o-redis-está-i.md`
  summary: O teste que leva o circuit breaker a `OPEN` (`RedisIdempotentRepositoryFailOpenITTest`) faz isso via container Redis pausado, onde toda chamada lança exceção de timeout — 100% de falha já excede o `failureRateThreshold` padrão (50%), então o teste não isola nem prova que o mecanismo de `slow-call-duration-threshold` (o que a AC #1 nomeia explicitamente) é, de fato, o que abre o circuito, distinto de uma abertura por taxa de falha genérica.
  evidence: Achado pelo Verification Gap Reviewer, com demonstração no código-fonte do resilience4j (`CircuitBreakerMetrics.checkIfThresholdsExceeded`) de que as duas condições (taxa de falha, taxa de chamada lenta) são avaliadas independentemente e qualquer uma abre o circuito. Isolar o mecanismo de "chamada lenta" exigiria uma chamada genuinamente lenta-mas-bem-sucedida (ex.: injeção de latência de rede via proxy), infraestrutura de teste que a story atual não construiu.

- source_spec: `_bmad-output/implementation-artifacts/3-8-tornar-a-política-de-falha-por-exceção-de-negócio-declarável.md`
  summary: O caminho `errorCallback.onErrorCondition(result)` em `IdempotentAspect.execute()` sempre remove a chave (`idempotentRepository.remove(idempotencyKey)`), ignorando a política `onBusinessException()` do método — um resultado sinalizado como erro por esse mecanismo nunca respeita `KEEP_FAILED`, só a exceção lançada no `catch` respeita.
  evidence: Achado de forma independente pelo Blind Hunter e pelo Edge Case Hunter na revisão desta story. Comportamento pré-existente e explicitamente fora do escopo declarado nas Dev Notes da própria story 3.8 ("Não confundir com o errorCallback/ErrorConditionalCallback já existente... Não misturar os dois mecanismos"). Unificar os dois modelos de sinalização de falha (exceção lançada vs. resultado flagueado) sob a mesma política é decisão de design que caberia a uma story própria.

- source_spec: `_bmad-output/implementation-artifacts/3-8-tornar-a-política-de-falha-por-exceção-de-negócio-declarável.md`
  summary: `KEEP_FAILED` reaproveita o mesmo `ttl`/`ttlTimeUnit` configurado para respostas de sucesso — não há como dar à falha cacheada uma janela de retenção diferente (ex.: mais curta), mesmo "repetir a mesma falha até o TTL expirar" sendo um trade-off bem diferente de cachear um sucesso.
  evidence: Achado pelo Blind Hunter na revisão desta story. Não é bug nem contradiz nenhum AC (a story só pede que a chave seja mantida e o erro reproduzido, sem exigir TTL configurável separado); é uma lacuna de produto que pode importar para consumidores reais de `KEEP_FAILED`.

- source_spec: `_bmad-output/implementation-artifacts/3-8-tornar-a-política-de-falha-por-exceção-de-negócio-declarável.md`
  summary: `IdempotentResponseWrapper#response` é declarado como `Object`; qualquer POJO customizado (não String/Number/Boolean) guardado ali sobrevive à ida ao Redis mas não à volta — `PolymorphicRedisSerializer` só registra o tipo concreto na raiz do que é serializado (`IdempotentRequestResponseWrapper`), então um campo `Object` aninhado desserializa como `LinkedHashMap` genérico via Jackson, nunca reconstrói o tipo original. Afeta **qualquer** consumidor que cacheia uma resposta de sucesso que seja um objeto complexo, não só `KEEP_FAILED` — nunca quebrou nada visível até agora porque, tipicamente, o retorno do método vai direto para o corpo de uma resposta HTTP, e `LinkedHashMap`/DTO original serializam para o mesmo JSON.
  evidence: Descoberto durante a implementação desta story: uma tentativa inicial de guardar `CachedBusinessFailure` (POJO de 2 campos String) como o `response` falhou com `AssertionFailedError: expected CachedBusinessFailure but was LinkedHashMap` num teste de integração contra Redis real (`RedisIdempotentRepositoryTopologyITTest`). Contornado nesta story guardando a falha como `String` codificada (única forma que sobrevive ao campo `Object` sem alteração na infraestrutura de serialização). Corrigir de verdade (ex.: `@JsonTypeInfo` no campo `response`, ou tipar `IdempotentResponseWrapper` por generics) é mudança na infraestrutura de cache compartilhada por todo o módulo, fora do escopo desta story — decisão do usuário (2026-08-30): registrar aqui em vez de expandir o escopo da 3.8.

- source_spec: `_bmad-output/implementation-artifacts/3-9-posicionar-o-aspecto-de-idempotência-fora-do-escopo-transaci.md`
  summary: Com `IdempotentAspect` agora deterministicamente fora (outer) do interceptor transacional do Spring, uma falha de **commit** (infraestrutura, não exceção de negócio) passa a cair de forma confiável dentro do mesmo bloco `catch` que aplica a política declarável `RELEASE`/`KEEP_FAILED` da Story 3.8 — sob `KEEP_FAILED`, uma falha transitória de commit pode ficar permanentemente cacheada como "falha de negócio", bloqueando retries legítimos que na verdade deveriam ser liberados (comportamento de infraestrutura, não de negócio).
  evidence: Achado pelo Edge Case Hunter na revisão desta story. Antes desta story, a ordenação entre `IdempotentAspect` e o interceptor transacional era não determinística — então esse caminho podia ou não ser alcançado por acidente. O `@Order(Ordered.HIGHEST_PRECEDENCE)` desta story, ao tornar `IdempotentAspect` deterministicamente outer, torna esse caminho sempre alcançável, mas a distinção entre exceção de negócio (deve respeitar `RELEASE`/`KEEP_FAILED`) e exceção de infraestrutura (`TransactionException`, deveria sempre liberar a chave, análogo ao fail-open da Story 3.7) não existe no código hoje. Fora do escopo desta story (Dev Notes proíbem introduzir mecanismo de compensação Redis/banco); é uma consequência emergente da interação 3.8 × 3.9 que merece uma story dedicada.

- source_spec: `_bmad-output/implementation-artifacts/3-9-posicionar-o-aspecto-de-idempotência-fora-do-escopo-transaci.md`
  summary: `RollbackTrackingTransactionManager` (fixture de teste nova) não expõe um método `reset()` e não limpa `wasRolledBack()`/`wasCommitted()` entre execuções — como o contexto Spring de teste (`TestAopTransactionalContext`) é cacheado entre testes da mesma classe por padrão, um teste futuro adicionado a `IdempotentAspectTransactionalRollbackTest` que reutilize o mesmo contexto herdaria estado obsoleto de um teste anterior sem forma de limpá-lo.
  evidence: Achado pelo Blind Hunter na revisão desta story. Não afeta os 2 testes atuais (cada um só depende de uma das duas flags, e nenhum depende do estado inicial `false`), mas é uma armadilha latente para quem adicionar um terceiro teste à mesma classe.

- source_spec: `_bmad-output/implementation-artifacts/3-10-tornar-o-namespace-de-prefixo-de-chave-configurável.md`
  summary: Os construtores públicos de `IdempotentAspect` que não passam por `ScosJdempotentConfig` (auto-configuração Spring) continuam construindo `DefaultKeyGenerator` sem namespace — um consumidor que monte `IdempotentAspect` programaticamente mantém o risco original de colisão de chaves entre aplicações, agora sem nenhuma variável de ambiente envolvida.
  evidence: Achado pelo Blind Hunter na revisão desta story. Reconhecido nas Completion Notes da própria 3.10 como fora de escopo (`IdempotentAspect` não está listado como arquivo modificado) — mudar a API pública de `IdempotentAspect` para exigir namespace em todos os construtores é decisão de design que extrapola esta story.

- source_spec: `_bmad-output/implementation-artifacts/3-10-tornar-o-namespace-de-prefixo-de-chave-configurável.md`
  summary: Não há documentação de consumidor (README do módulo `jdempotent` ou exemplo de `application.yml`) descrevendo a nova propriedade obrigatória `scos.jdempotent.namespace` — hoje só existe o bullet no CHANGELOG.
  evidence: Achado pelo Blind Hunter na revisão desta story, por comparação com outras entradas do mesmo CHANGELOG (ex.: mudanças do módulo `audit`) que apontam para um README correspondente. Não bloqueia a AC, mas reduz a chance de um consumidor notar a mudança breaking antes de atualizar a versão.

- source_spec: `_bmad-output/implementation-artifacts/3-10-tornar-o-namespace-de-prefixo-de-chave-configurável.md`
  summary: A Story 3.14 (migração `@Value` → `@ConfigurationProperties`, ainda `ready-for-dev`) não tem, em seu próprio arquivo, nenhum ponteiro para a decisão de coordenação registrada nas Dev Notes/Project Structure Notes da 3.10 sobre absorver ou não `ScosJdempotentProperties` — quem pegar a 3.14 pode não notar essa nota.
  evidence: Achado pelo Blind Hunter na revisão desta story. Risco de documentação ficar obsoleta/esquecida por estar apenas no lado da 3.10, não da 3.14.

- source_spec: `_bmad-output/implementation-artifacts/3-11-expor-métricas-de-idempotência.md`
  summary: Falta documentação para o consumidor sobre como efetivamente expor as métricas `idempotency.*` (ex.: dependência de `spring-boot-starter-actuator` para existir um bean `MeterRegistry`, `management.endpoints.web.exposure.include=metrics,prometheus`) — hoje só há explicação do mecanismo interno no CHANGELOG/Javadoc.
  evidence: Achado pelo Blind Hunter na revisão desta story. Fora do escopo desta story (instrumentação, AC #1); a Story 4.13 ("Documentar o módulo jdempotent") é o lugar natural para cobrir isso.

- source_spec: `_bmad-output/implementation-artifacts/3-12-introduzir-idempotencykeyresolver-com-composição-de-chave-de.md`
  summary: O construtor sem argumentos de `IdempotencyKeyResolver` cai silenciosamente para um `DefaultKeyGenerator` sem namespace, e não existe hoje nem um `@Bean` Spring nem um getter em `IdempotentAspect` que exponha a instância corretamente namespaçada que o aspecto usa internamente — um futuro entrypoint não-HTTP (Story 3.13, mensageria) não tem hoje uma forma segura e documentada de obter o mesmo resolver namespaçado, em vez de arriscar construir um sem namespace.
  evidence: Achado pelo Blind Hunter e pelo Edge Case Hunter na revisão desta story (mesma raiz, dois ângulos). Reproduz exatamente a armadilha que a Story 3.10 fechou para `DefaultKeyGenerator` (exigindo o namespace via configuração Spring, não removendo o construtor) — mas para `IdempotencyKeyResolver`, essa exigência ainda não existe em lugar nenhum. As próprias Dev Notes da 3.12 dizem que a 3.13 é quem constrói o entrypoint de mensageria; a forma de obter um resolver namespaçado fora do `IdempotentAspect` é decisão natural da 3.13, não da 3.12.

- source_spec: `_bmad-output/implementation-artifacts/3-12-introduzir-idempotencykeyresolver-com-composição-de-chave-de.md`
  summary: A nota `BREAKING` do CHANGELOG sobre a mudança de composição de chave (Story 3.12) explica o impacto (chave armazenada não é mais reproduzida por uma chamada equivalente) mas não dá nenhuma mitigação concreta para consumidores com TTL longo/indefinido, além de "planeje o rollout adequadamente".
  evidence: Achado pelo Blind Hunter na revisão desta story. Não é decisão trivial (a mitigação certa — janela de migração, script de flush de chaves antigas, marcador de versão na chave — depende de como cada consumidor usa o módulo), por isso não foi tratado como patch mecânico.

- source_spec: `_bmad-output/implementation-artifacts/3-13-suportar-header-idempotency-key-como-fonte-de-chave.md`
  summary: **Decisão explícita sobre `onMismatch` (pedida pelas Dev Notes desta story)**: `IdempotentKeyMismatchPolicy.CONFLICT` (novo atributo `@JdempotentResource#onMismatch()`) é, por enquanto, puramente declarativo — nenhum código desta story ramifica sobre o valor do atributo. Um header `Idempotency-Key` reenviado com um payload diferente já é hoje capturado pelo mecanismo pré-existente e agnóstico à origem da chave da Story 3.6 (`idempotentRepository.tryAcquire(key, payloadHash, ttl)` em `IdempotentAspect#execute()`), que lança `IdempotentPayloadMismatchException` (mapeado por módulos de integração para `422`/`PAYLOAD_MISMATCH`) — comprovado por `IdempotentAspectTest#given_idempotency_key_header_present_when_trigger_aspect_then_the_stored_key_is_header_derived`, que reenvia o mesmo header com corpos diferentes e recebe exatamente essa exceção, sem nenhum código novo de detecção. `CONFLICT` foi nomeado deliberadamente diferente de `PAYLOAD_MISMATCH` porque a convenção real de mercado para reuso de `Idempotency-Key` com corpo diferente é `409 Conflict` (ex.: semântica do Stripe), não `422` — uma story futura pode usar este atributo para fazer `IdempotentAspect` lançar um sinal de conflito distinto e específico de header, com esse mapeamento HTTP `409` diferenciado. Não implementado aqui porque nenhum AC/Task da Story 3.13 pede um tipo de exceção diferente — só que o atributo exista e a decisão fique documentada.
  evidence: Confirmado lendo o único uso de `tryAcquire`/`Lease.isMismatch()` em `IdempotentAspect.execute()` — a checagem de mismatch é incondicional e não inspeciona `keySource`/`onMismatch`, então já cobre o caso "mesma chave (de qualquer origem), payload diferente" sem alteração nesta story. Nenhum dos 4 testes pedidos pela Task 5 desta story (Idempotency-Key presente/ausente, contexto não-web, `X-Request-ID` nunca lido) exercita `onMismatch` — reforça que a story, como escrita, não exige a semântica de detecção em si, só a superfície de configuração + a documentação da decisão (ver Javadoc de `IdempotentKeyMismatchPolicy`). Também não fechado nesta story: o achado já registrado acima (Story 3.12) sobre o construtor sem-argumentos de `IdempotencyKeyResolver` cair silenciosamente para namespace vazio — esta story não introduziu um entrypoint de mensageria não-AOP separado (o método anotado continua passando pelo `IdempotentAspect`/AOP de sempre; só o contexto de request HTTP é que pode estar ausente), então a necessidade de obter um resolver namespaçado fora do `IdempotentAspect` continua sem solução, caso uma story futura precise dela de fato.

- source_spec: `_bmad-output/implementation-artifacts/3-14-migrar-configuração-para-configurationproperties.md`
  summary: O caminho de fallback `@PostConstruct validateExpirationTimeHour()` (para quando não há provider JSR-380 no classpath) não tem teste dedicado — `hibernate-validator` está sempre presente no escopo de teste do módulo, então `@NotNull`/`@Validated` sempre dispara primeiro e o fallback nunca é exercido.
  evidence: Achado pelo Blind Hunter na revisão desta story. O mesmo padrão de duas camadas foi introduzido pela Story 3.10 em `ScosJdempotentProperties` (mesma lacuna de cobertura lá, não introduzida por esta story) — a 3.14 apenas reaplicou um padrão já existente sem fechar a lacuna de teste original.

- source_spec: `_bmad-output/implementation-artifacts/3-14-migrar-configuração-para-configurationproperties.md`
  summary: Nenhum teste comprova que `@ConditionalOnProperty(scos.jdempotent.enabled)` ainda governa de fato a criação do bean/auto-configuração de `ScosJdempotentRedisProperties`/`ScosJdempotentConfig` — os testes atuais registram o bean diretamente via `.withBean(...)`, contornando o `@Conditional`.
  evidence: Achado pelo Blind Hunter na revisão desta story. A anotação `@ConditionalOnProperty` já existia antes desta migração (não foi alterada por ela) — lacuna pré-existente, apenas exposta incidentalmente pelos novos testes desta story.

- source_spec: `_bmad-output/implementation-artifacts/3-14-migrar-configuração-para-configurationproperties.md`
  summary: `expirationTimeHour` aceita silenciosamente valores zero ou negativos (`@NotNull` sem `@Positive`) — um TTL não positivo provavelmente quebra a semântica de expiração no Redis rio abaixo.
  evidence: Achado pelo Edge Case Hunter na revisão desta story. Comportamento pré-existente sob `@Value` (que também não validava positividade) — não é uma regressão introduzida pela migração desta story.

- source_spec: `_bmad-output/implementation-artifacts/3-14-migrar-configuração-para-configurationproperties.md`
  summary: A causa raiz corrigida por esta story (SPI registrado no arquivo descritor errado, então nunca executa silenciosamente) é uma classe de bug que pode se repetir em outros módulos deste foundation multi-módulo que usem as mesmas convenções `AutoConfiguration.imports`/`spring.factories`/`EnvironmentPostProcessor`.
  evidence: Achado pelo Blind Hunter na revisão desta story. Nenhuma auditoria repo-wide por outros `EnvironmentPostProcessor`/`ApplicationListener`/`ApplicationContextInitializer` mal registrados foi feita — fora do escopo do AC desta story, que é específico ao `ScosJdempotentRedisEnvironmentPostProcessor`.
