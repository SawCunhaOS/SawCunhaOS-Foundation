---
scope: Repositorio SawCunhaOS-Foundation inteiro (privacy, utils->decomposicao, exception->core/web, audit, jdempotent), altitude iniciativa/features, guiado pelo PRD 1.2.0 ja finalizado
purpose: Fixar os invariantes de arquitetura que o PRD 1.2.0 (F1 idempotencia, F2 exception, F3 decomposicao utils) precisa para execucao consistente, mais um novo piso obrigatorio de documentacao (Javadoc+comentario+diagrama Mermaid+README) repo-wide
altitude: iniciativa/features (mesma altitude do PRD)
updated: 2026-08-19T17:48
---

- (event) PRD 1.2.0 (prd-SawCunhaOS-Foundation-2026-08-18) carregado como input principal, status final. FRs 1-34 cobrem F1 jdempotent, F2 exception, F3 decomposicao utils
- (constraint) Investigacao do codigo real: Java 25, Maven multi-modulo (privacy->utils->exception->audit->jdempotent), publicado no Maven Central via Sonatype (perfil release com source+javadoc jar), groupId br.com.sawcunhaos, versao atual 1.2.0-SNAPSHOT
- (constraint) Checkstyle ja existe em etc/devops/checkstyle/checkstyle.xml com MissingJavadocMethod(scope=public,minLineCount=2) e MissingJavadocType(scope=protected) ja ativos no perfil 'analyze' de 4 dos 5 modulos atuais (jdempotent, audit, utils, exception) -- falta so em privacy. Isso confirma a leitura do FR-20 do PRD ('promovido de 4/5 poms'): FR-20 ja fecha essa lacuna
- (constraint) privacy (24 classes) ja tem Javadoc informal em 100% dos arquivos apesar de nao ter o perfil analyze/checkstyle habilitado -- nao e lacuna de conteudo, e lacuna de enforcement
- (constraint) Nenhum README de modulo (raiz, audit, privacy) contem diagrama Mermaid ou de qualquer outro tipo hoje. exception, jdempotent e utils nao tem README nenhum
- (decision) Modo de trabalho: Coaching path. Entregavel: spine (ARCHITECTURE-SPINE.md) + doc tecnico mais completo (guia de implementacao em prosa), publico = quem vai executar o PRD
- (decision) Piso de documentacao (pedido explicito do usuario, nao vem do PRD): todo codigo do repo -- nao so F1/F2/F3 -- passa a exigir Javadoc em API publica, comentario onde a logica nao e obvia, diagrama de fluxo de uso em Mermaid inline no README de cada modulo, e README para todo modulo (hoje ausente em exception/jdempotent/utils e nos modulos novos do F3)
- (decision) Enforcement: build-enforced via Checkstyle (regra ja existente) promovido a pluginManagement no POM pai (mesmo veiculo do FR-20/ArchUnit), aplicado a 100% dos modulos incluindo privacy. Comentario inline e diagrama Mermaid ficam fora do gate mecanico (nao ha ferramenta de lint para isso) e sao cobrados em code review
- (decision) Escopo do backfill: repositorio inteiro, incluindo audit e privacy que o PRD 1.2.0 nao toca funcionalmente. Usuario confirmou explicitamente que isso BLOQUEIA o release da 1.2.0 -- nao e item de backlog
- (assumption) Como privacy e audit ja tem Javadoc/README (so falta diagrama Mermaid +, no caso privacy, o gate de checkstyle), o backfill real de maior esforco esta nos modulos novos do F3 (~10 modulos saindo de utils) e nos 3 modulos hoje sem README (exception, jdempotent, utils-antes-de-sumir)
- (question) PRD 1.2.0 nao tem NFR nem estimativa de esforco cobrindo este piso de documentacao (Javadoc+diagrama+README) nem o fato de bloquear o release -- recomendo atualizar o PRD (novo NFR + ajuste de estimativa) para nao divergir da fonte. Perguntar ao usuario no fechamento
- (decision) AD-1 [ADOPTED] Fronteira/direcao de dependencia entre modulos, ratificado do FR-18/19/20/23 do PRD -- core sem deps externas, *-api so contrato, leaves nao sao dependidas por ninguem alem de apps
- (decision) AD-2 [ADOPTED] Contrato tryAcquire/Lease atomico + fail-open obrigatorio + UNIQUE constraint no banco como backstop real sob degradacao, ratificado de FR-1/2/4/25-29/NFR-6
- (decision) AD-3 [ADOPTED] Separacao contrato de dominio (core, sem Spring) x traducao HTTP (web, AutoConfiguration.imports), ratificado de FR-9-17/34
- (decision) AD-4 Piso de documentacao repo-wide (Javadoc build-enforced via Checkstyle ja existente promovido a pluginManagement + comentario review-gated + diagrama Mermaid + README por modulo), bloqueia release 1.2.0. Decisao nova desta spine, nao vem do PRD
- (version) ArchUnit archunit-junit5:1.5.0 verificado via web search em 2026-08-19 (release mais recente, ago/2026) -- unica dependencia nova desta spine
- (decision) AD-5 [ADOPTED] ArchUnit como mecanismo de imposicao, versao 1.5.0, regra nasce no mesmo commit que o modulo que protege (nunca big-bang final), ratificado de FR-19/20
- (assumption) Checkstyle ja configurado (configLocation) em 4/5 modulos mas o goal=check com failOnViolation nao foi encontrado localmente -- pode estar em pluginManagement do scos-bom (parent externo nao inspecionado). Marcado como verificacao pendente no AD-4, nao assumido como fato
- (event) Spine drafted (ARCHITECTURE-SPINE.md) a partir do memlog + investigacao do codigo real (grafo de dependencia atual confirmado via grep nos poms, coincide com a narrativa do PRD)
- (event) Reviewer Gate rodado: lint_spine.py (1 achado high, corrigido -- AD-5 sem Prevents), + rubric walker + version-verification + adversarial, todos como subagents paralelos
- (assumption) Version-verification reviewer corrigiu uma omissao real: scos-bom:1.3.1 estava disponivel localmente em ~/.m2 e nao foi inspecionado na primeira passada -- Spring Boot real e 4.1.0, Spring Cloud 2025.1.2, nenhum deles gerencia Resilience4j
- (version) Resilience4j para Spring Boot 4: artifact correto e resilience4j-spring-boot4 (nao -boot3), versao 2.4.0, verificado via WebSearch em 2026-08-19 -- e a segunda dependencia nova desta spine, nao so ArchUnit
- (decision) Achado adversarial AD-1: falta convencao de sub-pacote/nome canonico DENTRO de cada modulo (so a raiz de pacote e fixada) -- risco real de duas PRs simultaneas duplicarem entidade com nomes diferentes
- (decision) Achado adversarial AD-2: Lease/key nao tem contrato de FORMA (so de operacao) -- path HTTP vs mensageria podem derivar key/ttl de formas incompativeis, quebrando dedup cross-entrypoint silenciosamente
- (decision) Achado adversarial AD-5: regras ArchUnit cross-modulo (ex.: 'nenhum modulo depende de web') nao tem modulo-dono nem convencao de nome/pacote de teste -- risco de regra nunca escrita ou duplicada divergente
- (event) Rubric walker retornou: critical=1 (AD-1 diagrama sem aresta web->privacy, verificado em LoggingInitialFilter/LoggingFinalFilter reais que importam servlet+privacy), medium=2 (tensao AD-4 regra2 vs racional do AD-5; AD-4 regra5 trata gate nao-verificado como fato adotado). Resto do grafo brownfield confirmado correto
- (decision) Fix aplicado: AD-1 diagrama ganha aresta web-->privacy (LoggingInitialFilter/LoggingFinalFilter sao a prova real no codigo)
- (decision) Fix aplicado: AD-2 ganha regra explicita de forma do Lease/key (TTL como Duration, key sempre via IdempotencyKeyResolver unico independente de entrypoint HTTP/mensageria) -- fecha achado adversarial
- (decision) Fix aplicado: Consistency Conventions ganha linha de sub-pacote interno por modulo, ancorada no inventario congelado do FR-23 -- fecha achado adversarial de nomes duplicados
- (decision) Fix aplicado: AD-5 ganha regra de propriedade -- regras ArchUnit cross-modulo vivem em modulo de teste dedicado archtest, nao em modulo de implementacao individual -- fecha achado adversarial de regra sem dono
- (decision) Fix aplicado: Stack table corrigida -- resilience4j-spring-boot4:2.4.0 (nao -boot3, Spring Boot real e 4.1.0) adicionado como segunda dependencia nova, nao so ArchUnit
- (decision) Fix aplicado: AD-4 regra 2 reconhece a tensao com o racional do AD-5 como risco residual aceito; regra 5 desacopla a decisao de bloqueio (do usuario, valida independente) do mecanismo de gate (Checkstyle, ainda pendente de verificacao)
- (event) Reviewer Gate fechado: todos os 4 achados reais (1 critical do rubric, 3 do adversarial) corrigidos diretamente na spine. lint_spine.py limpo apos as edicoes
- (event) Triagem: nenhuma tag [ASSUMPTION] na spine (Coaching path, decisoes vieram do usuario). Unico item aberto e a verificacao pendente do Checkstyle failOnViolation no AD-4, ja desacoplada da decisao de bloqueio de release (nao bloqueia o fechamento da spine)
- (event) Guia de Implementacao (GUIA-DE-IMPLEMENTACAO.md) escrito -- doc mais completo pedido pelo usuario, publico=implementador. Polish (structure+prose) aplicado por autorrevisao: 1268 palavras, nenhum achado real, documento limpo
- (event) External handoffs: vazio no customize.toml, nada a executar
- (event) spine finalized
