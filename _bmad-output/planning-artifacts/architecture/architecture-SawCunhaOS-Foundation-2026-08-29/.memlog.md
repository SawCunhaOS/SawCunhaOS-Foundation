---
scope: Estrategia de testes do modulo jdempotent (parte Redis): topologias Standalone/Sentinel/Cluster, falha de subida com Redis desativado, teste de concorrencia real, TPS/throughput, separacao unit vs integration test
purpose: build-substrate
altitude: epic
updated: 2026-08-29T19:58
---

- (constraint) AD-2 (parent, feature spine 2026-08-19): aquisicao de lock do jdempotent so pode ser via tryAcquire(key, payloadHash, ttl) -> Lease, atomico via Lua script sobre Redis; Redis indisponivel e sempre fail-open, nunca fail-closed, protegido por circuit breaker (resilience4j-spring-boot4). Vincula qualquer teste de topologia/concorrencia desta spine a essas garantias, sem reabrir a decisao.
- (constraint) AD-1 (parent): direcao de dependencia core->nada, jdempotent depende de core+jdempotent-api+cache+privacy; nenhum modulo novo pode inverter essa direcao. Testes de infraestrutura Redis para jdempotent devem viver dentro do proprio modulo jdempotent (scope=test), nao criar modulo novo so para isso sem justificativa.
- (constraint) AD-5 (parent): ArchUnit (com.tngtech.archunit:archunit-junit5:1.5.0, scope test) e o mecanismo de imposicao de regras cross-modulo, promovido a pluginManagement do POM pai; regras cross-modulo vivem no modulo dedicado archtest, nunca duplicadas dentro de um modulo de implementacao.
- (constraint) AD-4 (parent): piso de documentacao obrigatorio (Javadoc, README, diagrama Mermaid) bloqueia release 1.2.0 para todo modulo, incluindo jdempotent.
- (version) Stack herdado (parent spine): Java 25, Spring Boot 4.1.0 (via scos-bom 1.3.1), Jackson 3 (jackson-bom 3.2.1), Jacoco minimo 80% nas areas tocadas (NFR-3).
- (question) Story 3.5 (tryAcquire/Lease, em review nesta sessao) implementou a aquisicao atomica via Spring Data setIfAbsent (SET NX PX), nao via Lua script como AD-2 exige literalmente. Divergencia real entre spine e implementacao atual, a resolver na review da 3.5 ou como amendment desta spine -- nao decidido ainda nesta run.
- (decision) Binds: separacao unit/integration test no modulo jdempotent. Prevents: Surefire (mvn test) subindo Docker/Testcontainers durante unit test, causando os erros de porta ja documentados nas Stories 3.1/3.4/3.5. Rule: maven-failsafe-plugin adicionado ao pom.xml do jdempotent, bindado a integration-test/verify; qualquer classe de teste que suba Testcontainers/Docker Compose usa sufixo *IT.java ou *ITTest.java e e excluida do maven-surefire-plugin; 'mvn test' roda so unit; 'mvn verify' roda unit+integration. Renomear PrimeNumbersJdempotentEnableTest/DisableTest (hoje sem sufixo IT, rodando incorretamente como unit test) para o padrao *ITTest.
- (decision) Binds: cobertura de topologia Redis (Standalone/Sentinel/Cluster) com infra real. Prevents: triplicar a mesma logica de asserts em 3 classes ITTest quase identicas. Rule: uma unica classe parametrizada (@ParameterizedTest + @MethodSource ou enum de topologia) monta a infra Testcontainers correspondente por topologia e roda o mesmo conjunto de asserts (tryAcquire atomico, contains, setResponse) contra as 3; complementa (nao substitui) os testes de contexto Spring puro ja existentes da Story 3.1 (ScosJdempotentRedisConfigurationTest), que continuam validando so a resolucao de RedisConfiguration sem infra real.
- (decision) Binds: teste de 'Redis desativado/mal configurado'. Prevents: erro tardio e obscuro (NPE em request real) quando a config de Redis esta invalida ou o Redis esta inalcancavel no startup. Rule: teste de contexto Spring (ApplicationContextRunner) prova que, com uma config de Redis invalida/inalcancavel, o contexto falha a subir (context.startupFailure() presente) e a causa raiz e identificavel na mensagem/causa da excecao -- fail-fast, nao fail-silent. Escopo != @ConditionalOnProperty(enabled=false) (esse caminho, jdempotent desligado deliberadamente, nao esta coberto por este AD e fica registrado em Deferred).
- (decision) Binds: teste de throughput/TPS do jdempotent contra Redis real. Prevents: gate de CI flaky por variacao de hardware/ambiente compartilhado. Rule: teste de carga via Testcontainers roda um numero fixo de operacoes tryAcquire concorrentes por um tempo fixo e LOGA o TPS medido (assert apenas de ausencia de erro/excecao, nunca um piso minimo de TPS); e informativo, nao bloqueia build; sufixo *ITTest, mesma fase Failsafe dos demais testes de integracao.
- (decision) Binds: escopo desta spine. Rule: o padrao Failsafe+sufixo IT desta spine se aplica so ao modulo jdempotent nesta rodada; o modulo audit (tambem usa Testcontainers hoje, mesma dor potencial) fica registrado em Deferred para uma story futura aplicar o mesmo padrao, nao expandido para convencao repo-wide na spine pai agora.
- (decision) Binds: empacotamento em story. Rule: todo este escopo (split unit/IT + topologia parametrizada + fail-fast em config invalida + concorrencia real + TPS informativo) vira UMA story coesa no Epico 3 (proximo numero livre na sequencia 3.x), nao dividida em multiplas stories -- e um unico objetivo de qualidade de teste do jdempotent/Redis.
- (constraint) Story 3.5 ja introduziu InMemoryIdempotentRepositoryTryAcquireTest (concorrencia via ExecutorService, sem Testcontainers) e RedisIdempotentRepositoryTryAcquireITTest (concorrencia real via GenericContainer avulso, ja com sufixo ITTest mas sem Failsafe bindado ainda). A nova story desta spine deve migrar/absorver esses arquivos para o novo padrao Failsafe em vez de recriar testes de concorrencia do zero.
- (event) Reviewer gate rodou: verify-tech-versions (limpo) e adversarial (5 achados: AD-6 padrao de include/exclude Failsafe/Surefire nao reconhece *ITTest.java por default, gap de CI nao rodando mvn verify, AD-2 herdado sem operacionalizacao de fail-open/circuit-breaker, vocabulario ambiguo em AD-7 sugerindo testar fluxo antigo contains/setResponse, ambiguidade de lifecycle de containers e desenho de AD-9).
- (decision) Fix aplicado AD-6: sufixo unico *ITTest.java (nao dois sufixos); configuracao explicita <excludes> no Surefire e <includes> no Failsafe (defaults de nenhum dos dois plugins reconhecem *ITTest.java corretamente); CI (.github/workflows/build.yml linhas 45 e 112) precisa trocar 'mvn test'/'mvn -Panalyze test' por 'mvn verify'/'mvn -Panalyze verify' para o jdempotent, senao AD-6 para de rodar os testes de integracao em CI (regressao silenciosa).
- (decision) Fix aplicado AD-7: asserts da topologia parametrizada so via tryAcquire/Lease (nunca contains/store/setResponse isolados, que sao o fluxo antigo proibido pelo AD-2 herdado); lifecycle de containers esclarecido (um container por topologia, static/classe, compartilhado entre metodos parametrizados da mesma topologia).
- (decision) Fix aplicado AD-9: numero fixo de operacoes (nao janela de tempo fixa) para dar denominador deterministico ao calculo de TPS logado.
- (question) AD-8 tem conflito real e nao resolvido com AD-2 herdado: AD-2 exige fail-open SEMPRE quando o Redis esta indisponivel (nunca fail-closed); a resposta do usuario na coaching session pediu fail-fast (app nao sobe) quando o Redis esta inalcancavel. Investigacao confirmou que ScosJdempotentRedisConfiguration nao configura validateConnection=true no LettuceClientConfiguration -- a conexao e lazy, entao 'Redis inalcancavel' hoje NAO causa falha de contexto (fail-fast tecnicamente impossivel sem mudar codigo de producao, o que tambem colidiria filosoficamente com o fail-open do AD-2). Config invalida (propriedades malformadas) e um caso diferente, testavel sem mudar producao. Decisao pendente do usuario, nao resolvida silenciosamente nesta run.
- (decision) AD-8 fechado: escopo restrito a config invalida/malformada (testavel sem mudar producao); 'Redis alcancavel mas fora do ar' fica fail-open (AD-2) e sem teste de fail-fast, decisao explicita do usuario para nao colidir com a arquitetura ja adotada.
- (event) spine finalized
- (decision) Amendment AD-6 (mantendo AD-id estavel): em vez de trocar a fase dos 2 steps globais de CI (o que rodaria verify no reactor inteiro), adicionar UM step novo escopado 'mvn -B -pl jdempotent -am verify' logo apos o step 'Run tests' (linha 45) do build.yml -- satisfaz o requisito (testes de integracao continuam rodando em CI) sem afetar tempo/comportamento dos demais modulos. Decidido durante a autoria da story 3.18 (bmad-build), antes de qualquer implementacao.
